# PROJECT STATE

## DEV — WAVE 2: VIBE-CODED PURGE (CHEAP PARTS) + LANDING FOOTER — 2026-09-01

Built per Cato's Wave 2 scope above (item 7's cheap parts + item 2's footer gap). Explicitly did not touch the brand gradient and did not do a broader redesign, per Cato's constraint. Branch `claude/futureos-student-app-3ewdz6`, this worktree.

**1) Emoji-in-headings — removed, judgment applied per-site, not a blind strip.**
- `StudentHome.tsx`/`BusinessHome.tsx`: removed the trailing 👋 from the `<h1>` greeting (the exact "Welcome...! 🚀"-style tell the CEO named).
- `missions/page.tsx`: `CATEGORY_TITLES` (`<h2>` section titles) had emoji prefixes (`📚 School Missions`, etc.) — removed; the category icon is already shown per-item in `MissionListItem`, so the heading emoji was redundant, not just decorative.
- `weekly-review/page.tsx`: the 4 student card-section titles (`📚 School`, `🧠 Skills`, `🚀 Future`, `🔥 Consistency`) were raw emoji glyphs while the sibling business "Plan" section already used a real `lucide-react` `Target` icon — a real inconsistency, not just an emoji tell. Fixed by swapping all 4 to matching lucide icons (`BookOpen`, `Brain`, `Rocket`, `Flame`) at the same size/color as the existing `Target` icon, making both tracks consistent instead of just deleting the icon.
- **Left untouched, deliberately:** every emoji used as the app's de facto icon system (subject icons, career icons, achievement badges, mission category tags, mastery-status dots, interest picker, the streak 🔥 in `StatTile`/`StudentHome`/`BusinessHome`/`BrandPanel`). Grepped the full remaining emoji list after the pass and confirmed every remaining instance is a functional icon substitute, not heading/title copy.

**2) Em-dashes in user-facing copy — rewritten sentence-by-sentence, not find-replaced.**
Fixed em-dashes in every genuine JSX text node / user-visible string I could find across: landing page (`page.tsx`), `BrandPanel.tsx` (all 4 auth/onboarding variants' hero quotes + the "82% match" stat), signup/login/choose-plan/auth-callback error and helper copy, both onboarding flows (`StudentOnboarding.tsx`/`BusinessOnboarding.tsx`), the upgrade/pricing page, `PaywallGate.tsx`, homework-help quick-action prompts (both the current and legacy subject-scoped versions — these become the literal text sent to the AI Coach, so worth fixing even though they're "prompt" text), quiz/exam pages, weak-topics, flashcard review results, missions detail page, profile skill list, root `layout.tsx` `<title>` metadata and `branding.ts`'s meta `description`, and 3 catalog content strings (`careers.ts`'s `whyItMatches`/`educationRoutes`, `roadmap.ts` level 2 description, `missions.ts` a mission description) that render as real in-app copy. ~60 individual rewrites, each read and restructured on its own (periods, commas, colons, or the app's existing `·` separator convention for short stat-style labels — not a mechanical dash-to-comma swap).
- **What I didn't get to, and why:** ~77 remaining em-dash instances live in `src/app/api/**/route.ts` — the large majority are Claude system-prompt instruction text (never rendered to a user, sent only to the AI) and a smaller number of thrown `NextResponse.json({ error: ... })` strings that are mostly caught and replaced with generic UI copy rather than rendered verbatim. Per the brief's own prioritization guidance (get the highest-visibility copy first, it's fine to not do a full sweep of buried backend strings), I left these. Also left all remaining em-dashes that are inside `/**...*/` or `//` code comments (dozens, checked file-by-file, confirmed not code that ends up on screen) and the one `"—"` glyph in `school/progress/page.tsx` used as a legitimate "no data yet" placeholder value (not a sentence construction), which is a different, pre-existing UI convention, not the copy issue this pass targets.

**3) Page-load fade — judgment call: reduced on the 5 highest-traffic screens, kept everywhere else.**
`animate-fade-in` (`tailwind.config.ts`: plain `opacity 0→1`, 0.2s ease-out, no translate/scale, no stagger) is genuinely subtle in isolation — I don't think a single instance of it is a "vibe-coded" tell on its own, and said so rather than reflexively stripping it everywhere. But tracing where it's actually mounted matters more than the animation itself: Next.js App Router unmounts/remounts a route's page component on every navigation between different routes (it doesn't persist across route changes the way a shared layout does), so the 5 main bottom-nav/sidebar tab destinations (`StudentHome`/`BusinessHome`, `school/layout.tsx` which wraps the whole School/Plan tab, `StudentFutureHome`/`BusinessGrowHome`, `coach/page.tsx`, `profile/page.tsx`) were re-triggering this fade on **every single tab tap, for the entire lifetime of a session** — dozens of times a day, not once per page load. That repetition, not any individual fade, is what plausibly reads as templated. Removed `animate-fade-in` from those 5 files only. Left it everywhere else it was already used (deeper/secondary routes — `missions`, `missions/[id]`, `weekly-review`, `upgrade`, `deadlines`, `homework-help`, `session`, `plan/new`, `plan/[planId]`, `future/[slug]` — plus `Modal.tsx`/`Tooltip.tsx` micro-interactions and onboarding, all of which are visited far less often per session and where a one-time "new context has loaded" fade is a reasonable, not-templated-feeling transition).

**4) Landing page footer — added.** `src/app/page.tsx` restructured from a single flex-row (`hero column` + `BrandPanel`) into a flex-col wrapping [the same flex-row, unchanged, now inside its own wrapper div] + a new full-width `<footer>` below it, so the footer spans under both the hero column and the desktop `BrandPanel` rather than being squeezed into the left column. Footer: `LogoMark` + brand name, 3 links (Privacy/Terms/FAQ), copyright line with `new Date().getFullYear()`. Reused only existing typography/spacing/color tokens (`text-sm`/`text-xs`, `text-muted-foreground`, `border-border`) — no new design-system additions, brand gradient untouched.
- **Privacy/Terms/FAQ:** scaffolded real stub pages (`src/app/privacy/page.tsx`, `/terms`, `/faq`) via one new shared shell (`src/components/shared/StaticContentPage.tsx`) rather than shipping dead `href="/privacy"` links that 404 today. Privacy/Terms stubs are deliberately non-committal placeholder copy ("full policy is being finalized and will be published here") — I did not author actual legal claims or terms, per Cato's own flag that legal content needs CEO review, not an agent's invention. FAQ content is factual product description I could verify directly against the shipped code (free-tier vs. Plus gating, track-lock-at-onboarding) and is safe to publish as-is; still worth a CEO/Product glance before calling FAQ "final," but it isn't legal content.

**Verification:**
- `npx tsc --noEmit`: clean (exit 0), run directly.
- `npx next build`: clean (exit 0), all 46 routes including the 3 new stub routes (`/privacy`, `/terms`, `/faq` at 181 B each).
- Playwright (Chromium at `/opt/pw-browsers/chromium-1194`, real compiled CSS from this repo, `next start` against the production build): landing page screenshotted at 390px and 1280px, dark and light (theme resolved via `localStorage` + the app's own `themeInitScript`, not guessed) — footer renders full-width, correctly under `BrandPanel` on desktop, no overflow/wrap/collision with the hero content in any of the 4 combinations. `/privacy` (390px) and `/faq` (1280px) also screenshotted and render cleanly.
- Did **not** get a live-authenticated screenshot of the emoji/icon-swap or fade-removal changes (`StudentHome`, `weekly-review`, etc.) — this sandbox has the same Supabase-egress restriction noted in the prior full-app QA pass, so there's no way to reach a real logged-in session here. Confirmed instead via `curl` that `/app`, `/app/weekly-review`, `/app/missions` all return 200 (no server crash) against the production build, plus the clean `tsc`/`build`, plus the changes themselves are simple (icon swap, class removal, text edits) with no new logic. Flagging this gap explicitly rather than claiming a visual check that didn't happen — QA has live-DB access in a prior pass and should do the authenticated visual pass this session couldn't.

Files touched: `src/app/page.tsx`, `src/app/privacy/page.tsx` (new), `src/app/terms/page.tsx` (new), `src/app/faq/page.tsx` (new), `src/components/shared/StaticContentPage.tsx` (new), `src/components/shared/BrandPanel.tsx`, `src/components/shared/PaywallGate.tsx`, `src/lib/branding.ts`, `src/lib/catalog/careers.ts`, `src/lib/catalog/missions.ts`, `src/lib/catalog/roadmap.ts`, `src/app/layout.tsx`, `src/app/login/page.tsx`, `src/app/signup/page.tsx`, `src/app/choose-plan/page.tsx`, `src/app/auth/callback/page.tsx`, `src/app/onboarding/StudentOnboarding.tsx`, `src/app/onboarding/BusinessOnboarding.tsx`, `src/app/app/StudentHome.tsx`, `src/app/app/BusinessHome.tsx`, `src/app/app/coach/page.tsx`, `src/app/app/profile/page.tsx`, `src/app/app/upgrade/page.tsx`, `src/app/app/weekly-review/page.tsx`, `src/app/app/missions/page.tsx`, `src/app/app/missions/[id]/page.tsx`, `src/app/app/school/layout.tsx`, `src/app/app/future/StudentFutureHome.tsx`, `src/app/app/future/BusinessGrowHome.tsx`, and the school/study-flow pages listed in the em-dash section above.

BLOCKERS: None. NEXT: QA review, including the authenticated-screen visual pass this session couldn't do.

---

## CATO — TRIAGE + PHASED PLAN FOR CEO'S 17-ITEM BUG/FEATURE REPORT — 2026-09-01

CEO sent a large, unstructured 17-item report (bugs + features + a full launch-readiness checklist). This section is Cato's investigation of what's actually true in the code right now for each item, followed by a phased execution plan. Nothing in this section has been built yet — this is planning only, per instruction. **Housekeeping note:** `PROJECT_STATE.md` was several commits stale at the tail before this pass — the last few entries in the file (ending at QA's round-3 signup-OTP review) predate `aa0c574`/`43261a9`/`5b7758f`/`99a076b` (cross-account profile-leak fix, email-confirmation-link routed through `/auth/callback`, **business track marked "coming soon" on `/choose-plan`**, and **Google Sign-In removed entirely**). All of those are real, merged, on `HEAD` (`99a076b`) — verified via `git log`, not assumed. The business-track pause is relevant to several items below and is called out where it matters.

### Item-by-item findings

**1) "Fake info when first logged in" — REAL, and worse than it sounds.** `src/lib/onboarding/completeOnboarding.ts` inserts a full fabricated `timetable_entries`/`homework`/`exams`/`study_sessions` set for every new student, straight into the real per-user tables, at signup. Two paths: `src/lib/onboarding/researchSchool.ts` tries an AI web-search call to *guess* a plausible curriculum/timetable for the student's actual school/year/subjects (`src/app/api/onboarding/research-school/route.ts`), and if that fails/times out (55s bound, "routinely takes 25-40s+" per its own code comment — fails often) it silently falls back to `src/lib/seed/demoData.ts`'s `buildDemoData()`, which is fully generic (rotates through the student's chosen subjects with template assignment titles like "Lab Report", "Algebra Practice Set"). **Neither path is the student's real homework/exams/timetable.** Confirmed via grep: there is **no delete UI anywhere** for `homework`, `exams`, or `timetable_entries` (only `study_materials`/`study_plans`/`study_subjects` have delete). A student can add real items on top (Deadlines-initiative forms) and mark homework complete, but the fabricated exams and timetable are **permanent and unremovable** — this is why it looks fake *every single login*, forever, exactly as the CEO described. This is a real, first-priority product-integrity bug, not a misread empty state.

**2) "No landing page" — STALE as literally stated, but with a real, narrower gap underneath.** `src/app/page.tsx` (post round-3 UI redesign) is a genuine, well-composed landing screen: `LogoMark`, headline, subcopy, primary CTA, a real proof strip, and a desktop `BrandPanel` two-column layout — this is not the empty centered-column complaint round 3 was built to fix. But it is a **single hero screen, not a marketing page**: no nav bar, no footer, no pricing/features/FAQ section, and critically **no links to Privacy/Terms/FAQ anywhere** (confirmed: those pages don't exist at all, see item 6). So "no landing page" is inaccurate; "the landing page has no footer, no FAQ, no legal links, and nothing to link to even if it did" is accurate.

**3) Ownership checks — SOLID, verified directly, not just carried forward from old QA notes.** Read all 16 `src/app/api/*/route.ts` files: every single one calls `requireUser(req)` first (JWT-verified via `supabase.auth.getUser()`, never a client-supplied id) and every DB read/write is scoped by `.eq("user_id", user.id)` / `.eq("id", user.id)`, with RLS as an independent backstop (already impersonation-tested live against production on 2026-08-27, unchanged schema since). The two service-role writes that exist (`create-subscription`, the Stripe webhook) are both scoped to `user.id` from a verified session, or authenticated via Stripe's own signature (webhook has no user session by design). No bypasses found.

**4) Parameterized queries — SOLID.** Grepped the entire codebase for raw SQL, string-concatenated queries, and `.rpc()` calls: **zero matches.** Every single database call goes through the Supabase JS client's query builder (`.select()`/`.insert()`/`.eq()`/etc.), which is inherently parameterized. SQL injection is structurally not possible via this pattern anywhere in the app.

**5) Key separation — SOLID.** `src/lib/supabase/client.ts` (the only browser-facing Supabase client) uses only `NEXT_PUBLIC_SUPABASE_ANON_KEY`. `SUPABASE_SERVICE_ROLE_KEY` is referenced in exactly 3 files, all server-only (`src/lib/supabase/server.ts`, and the two routes above), none of them client components (grepped for `"use client"` + the service-role key together — zero hits). No leak found.

**Items 3-5 verdict: no code changes needed.** This is a real, clean finding — say so plainly rather than inventing work.

**6) The ~35-item launch checklist — gone through individually, not padded:**
- **Already done:** onboarding (exists, if seeding fake data per item 1), sign up/log in (exists, hardened through 4 real QA rounds), email verification (OTP + link-via-`/auth/callback`, both shipped), data persistence (Supabase Postgres, verified), payment flow (Stripe subscription + webhook + portal, code-solid), user permissions/ownership (see items 3-5), loading states (`LoadingScreen` used at 19+ sites), meta titles/descriptions (root `layout.tsx` has real `title`/`description` via `branding`), favicon (Next `icon.png` convention + `apple-touch-icon` in `public/icons`), mobile responsiveness (extensively hardened — 4+ real overflow bugs found and fixed across QA history), accessibility — partial (`:focus-visible` ring shipped app-wide in round 1; the app's one `<img>`-equivalent, `LogoMark`, correctly uses `alt=""` as decorative; but only 11 `aria-label` uses total and no accessibility audit has ever been run).
- **Partially done / real gaps found by code, not assumed:**
  - **Empty states:** genuinely built (`EmptyState` component, ~20 adopted sites per prior QA).
  - **Error states / network states: real, systemic gap.** `useTableRows` (the hook every list screen reads through) exposes `{data, loading, error}` — but grepped every consuming page in `src/app`: **zero of them ever destructure or render `error`.** A genuine fetch failure (RLS misconfig, outage, dropped connection) renders identically to "you have nothing" — the user can never tell the difference. This is worth fixing; it's small (the plumbing already exists, it's just never read).
  - **Responsiveness:** "all devices" is doing a lot of work — only 390px/1280px have real Playwright coverage across QA history; no tablet-width (768-1024px) verification has ever been done.
- **Not done / genuine gaps:**
  - **Password reset:** **does not exist at all.** No `resetPasswordForEmail` call, no `/reset-password` route, no "Forgot password?" link on `/login` (grepped, confirmed absent). A user who forgets their password has zero self-service recovery. This is a severe, real gap for a production app with real users.
  - **Account deletion:** does not exist anywhere (grepped, confirmed).
  - Custom 404 (`not-found.tsx`) and error boundary (`error.tsx`): neither exists — the app falls through to Next's bare defaults.
  - Privacy policy, Terms page, FAQ: none exist as pages or content.
  - robots.txt, sitemap.xml: neither exists (checked both the literal-file and Next's `robots.ts`/`sitemap.ts` app-router conventions — zero).
  - OG tags / social share preview, canonical tags: `layout.tsx`'s `metadata` object has no `openGraph`/`twitter`/`alternates.canonical` — sharing a link to Alxioum today renders with no preview card.
  - Cookie consent banner: does not exist.
  - Google Analytics / any analytics: **zero analytics of any kind** in the codebase — no `gtag`, no GA snippet, nothing.
  - Crash reporting: none (no Sentry/Bugsnag/equivalent in `package.json` or code).
- **Out of scope right now, flagged not padded:** beta testers is a CEO/go-to-market action (recruit real people), not a code task — nothing to build. "Test forms" / "check broken links" / "optimize performance" are audit *activities*, not fixed-scope deliverables — sized separately below, not bundled into a checkbox.
- **Real tension worth flagging, not silently complying with:** the CEO's own item 6 says "load GA before the cookie banner" — doing that literally means GA can set tracking cookies before the user has consented, which is the specific thing cookie-consent banners exist to prevent (and this is a student app that may have minors as users — already flagged as a live privacy concern in the Deadlines-initiative history when the achievements-leaderboard idea was rejected for the same reason). Recommend GA loads in a consent-gated/anonymized mode instead, not literally "before" consent — flagging for a CEO call, not silently building the literal ask.

**7) "Vibe-coded" tells — checked each one named, precisely, against the current shipped code:**
- **Purple-to-blue/cyan gradient — REAL, and it's the core brand identity, not a stray bug.** `globals.css`: `--accent: 252 92% 68%` (indigo/purple) → `--accent-end: 186 84% 58%` (cyan). Used in `bg-gradient-brand`/`text-gradient-brand` at **37 sites** across the app (buttons, headings, glows). This is a real, if very large-scope, change — it's not a one-line fix, it's the brand.
- **Gradient hero text — REAL.** Landing page headline: `<span className="text-gradient-brand">grades.</span>` — one word gradiented, exactly the pattern round 3's own diagnosis flagged as an "AI slop" tell (and round 3 explicitly chose not to touch this).
- **Emojis in headings — REAL, confirmed by direct string search, not a hunch.** `StudentHome.tsx`/`BusinessHome.tsx` greeting headings end in `👋`; `weekly-review/page.tsx` and `missions/page.tsx` section headings are prefixed `📚 School`, `🧠 Skills`, `🚀 Future`, `🔥 Consistency`, `💼 Business Missions`, etc. Beyond headings specifically, emoji is used as the app's de facto icon system in many more places (subject icons, interest tags, streak flame) — a broader pattern than just headings.
- **Inter font everywhere — REAL, confirmed.** `layout.tsx` loads `Inter` from `next/font/google` as the only font, app-wide, no exceptions.
- **Colored-border cards — REAL but contained, not indiscriminate.** The base `Card` component (`src/components/ui/Card.tsx`) has no colored border by default. The colored-border pattern (`border-accent/20`, `border-accent/30`, `border-success/40`) is opt-in and concentrated on ~10-12 specific "hero"/featured cards (Home dashboards, the upgrade/pricing page) — real, worth revisiting, but not applied to every card in the app the way the complaint might imply.
- **Fade-in-on-scroll — NOT what's actually there; a narrower, milder pattern is.** Grepped for `whileInView`/`IntersectionObserver` (the actual scroll-triggered-reveal mechanism): **zero matches.** What exists instead is `animate-fade-in`, a plain page-mount transition applied to ~25 page-wrapper `<div>`s (the whole screen fades in once on load, not per-element as you scroll). This is a real, if much milder, instance of "everything fades" — correcting the record before scoping it: don't build a fix for scroll-triggered animation that doesn't exist; the actual, smaller fix is reconsidering the blanket page-load fade. (The literal `FadeIn.tsx` component some agents flagged before is dead/unused code, unrelated to this pattern — confirmed unimported again.)
- **Buttons that fade on hover — MOSTLY NOT TRUE.** The shared `Button` component (used for essentially every real button in the app) uses `hover:brightness-110` (primary/mission) or `hover:bg-*` (secondary/ghost/outline) — only the rarely-used `danger` variant uses `hover:opacity-90`. Grepped `hover:opacity` app-wide: 3 total matches, 2 of which are deliberate glow-blur hover effects (nav icons), not button fades. This specific complaint does not match current reality.
- **Em dashes everywhere in copy — REAL and pervasive.** Sampled and confirmed genuinely in user-facing strings (not just code comments): error messages, empty-state subtitles, placeholder text, AI-tutor prompt framing, and the landing page's own hero subcopy all use em-dash constructions — dozens of real instances across the app. Notably, this project's own `PROJECT_STATE.md` history (written by prior agent rounds) is also saturated with em dashes, which is a good tell that this is a systemic "AI voice" pattern worth a deliberate copy pass, not a one-off.

**8) "How old are you" — REAL gap, but it's a decision, not just a form field.** No age/birthdate field exists anywhere (only "year group," a school-grade proxy, which doesn't map cleanly to age across countries/curricula). Before building: **does this gate anything** (e.g. under-13 handling, parental consent, COPPA-style restriction) or is it purely informational/analytics? This app has already flagged minors-as-users as a live privacy consideration once before (the rejected achievements-leaderboard idea, Deadlines-initiative history). If it's meant to gate something, that's a real compliance decision, not a UI task — flagging for a CEO call before Wave 4 touches this.

**9) Tutorial for new users — REAL, fully missing.** Grepped for any tour/walkthrough/coachmark library or pattern: zero. After the onboarding Q&A, a user lands straight in `/app` with no guided introduction to the 5 tabs or any feature.

**10) "Add to home screen" mobile instructions — REAL gap, but the hard infra is already done.** Full PWA plumbing already exists and works: `public/manifest.json` (real icons, `display: standalone`), `public/sw.js` (a real, deliberately network-first service worker), `<meta appleWebApp>` in `layout.tsx`. What's missing is purely the **in-app UI** teaching a mobile visitor how to actually install it — genuinely zero UI for this anywhere (grepped). This is a small, well-scoped addition on top of infra that already works, not a build-from-zero.

**11) Roadmap/career page unclear next step — REAL, and worse than "unclear": it's structurally inert.** `roadmap_progress` (the table backing the Future-tab roadmap timeline) is written to **exactly once in the entire codebase** — at onboarding (`completeOnboarding.ts`, level 1 auto-completed, level 2 auto-unlocked) — and never updated again anywhere. Every user's roadmap is permanently frozen after signup; there is no action anywhere in the app that ever advances a level. The 6 roadmap levels (`src/lib/catalog/roadmap.ts`) are static descriptive text ("Build your first project") with no linked action/checklist/CTA. This isn't just unclear copy — the feature has no mechanism to progress at all.

**12) Coach chat naming — HALF ALREADY DONE, verify-before-build worked exactly as intended.** AI auto-naming: **already shipped and working** — `src/app/api/coach/route.ts` fires a dedicated Claude call ("Give a 3-6 word title summarizing this conversation") the first time a thread gets a reply, and writes it to `chat_threads.title`. User-initiated rename: **does not exist** — grepped the whole Coach page for any rename affordance (edit icon, editable title, update call for `title`) — none found, thread titles are read-only in the UI. Build only the missing half.

**13) "Homework tab isn't there" — REAL, precise structural gap, confirmed.** `SchoolSubNav.tsx` has 6 tabs: Home/Subjects/Exams/Flashcards/Quizzes/Progress — **no Homework tab**, while Exams (a very similar due-date-bearing item type) got promoted to its own full sub-nav tab and dedicated `/app/school/exams` page. Homework only exists buried inside `StudentSchoolHome.tsx` (the School "Home" sub-view) with no standalone list page. This is exactly the asymmetry the CEO is describing, not a misunderstanding.

**14) Add homework/lessons/exams + mock-exam-from-past-paper — split findings, size varies a lot by sub-part.**
- Adding homework/exams: **already exists** (built during an earlier reconciliation round, item 1's "no add-exam/homework UI" QA bug, now fixed).
- Adding "the day's lessons" (timetable): **does not exist** — `timetable_entries` is read-only everywhere, only ever written once at onboarding (ties directly back to item 1 — this is the same permanent-fake-data problem for the timetable specifically). Real, and worth bundling with item 1's fix rather than treating separately.
- **Mock-exam-from-past-paper: partially overlaps existing infra, but is a genuinely new capability, not a duplicate.** There is already a real "Exam Mode" (`src/app/app/school/quizzes/exam-mode/page.tsx`) — a timed, AI-generated quiz at exam-level difficulty, tied to a subject. But it generates generically from the subject's curriculum, **not** from an uploaded/pasted past paper's actual content/style, which is specifically what the CEO is asking for. The building blocks exist (the `study_materials` upload pipeline already supports paste-text/PDF/photo upload + AI analysis, see item 15) but the "generate an exam grounded in *this specific* uploaded paper" generation path does not exist and needs a new AI route. Medium-sized, not small, not a full new subsystem.

**15) "Notes" feature (paste notes / book photos → AI organizes them) — ALREADY BUILT, this is exactly the "verify before duplicating" case the brief called out.** `src/app/app/school/subjects/[subjectId]/materials/new/page.tsx` already supports pasted text, typed notes, PDF upload, and **photo upload** (`accept="image/*"`, i.e. book-page photos). `src/app/api/study/analyze-material/route.ts` already runs this through Claude and extracts topics/summaries/key concepts/terms/practice questions, saved to `study_topics` and viewable later. **Do not build parallel infrastructure.** The real, narrow gaps: (a) it's subject-scoped (requires picking/creating a `study_subject` first) rather than a flat, always-available "Notes" entry point, and (b) it's not labeled or discoverable as "Notes" anywhere in the UI/nav — it lives under Subjects → [subject] → Materials. If the CEO's intent is "organized notes" specifically (vs. the current topic/term/quiz-question extraction framing), that's a copy/output-shape tweak on an existing feature, not a new build.

**16) Achievements "fixed so they work" — mechanism is NOT broken; the actual problem is silence, not malfunction.** Traced all 9 achievement definitions to their award call sites: every one has a real, reachable trigger condition (first study session, 10 sessions, first quiz, first mock exam, 20 mastered flashcards, 7-day streak, first career mission, first project, career path chosen), all idempotent via a unique-key upsert, all correctly rendered locked/unlocked (with a lock icon + grayscale) on the Profile page. **What's actually missing: zero celebratory feedback anywhere** — grepped for any unlock toast/modal/notification: none. An achievement is awarded completely silently; the only way to notice is to manually check Profile later, which reads exactly like "achievements don't work" from a user's seat even though the backend is fine. Secondary, smaller findings: `career_path_chosen` is awarded to nearly every user automatically at onboarding (barely an "achievement"), and the business track has zero business-flavored achievements (all track-specific ones are `student`-only) — lower priority given business signups are currently paused.

**17) Notifications system — genuinely does not exist, confirmed, not assumed.** No notification bell/center UI anywhere (grepped). No push infrastructure (`sw.js` has zero `push`/`notificationclick` listeners — it's a static-asset cache only). No general transactional-email capability in app code — `resend` isn't even an npm dependency; Resend today is purely a Supabase Auth SMTP provider configured in the dashboard for auth emails (OTP/confirmation), not something app code can call for arbitrary notifications (exam reminders, etc.) without adding it as a real new integration. **This needs a CEO decision before any build**, not a guess: push (needs a new service — no push infra exists at all today, this project has repeatedly avoided adding new external delivery infra) vs. email (Resend already exists for auth, more feasible to extend, but is a real new integration for arbitrary app-triggered sends) vs. in-app-only (cheapest, matches the precedent the Deadlines initiative already set of deliberately choosing in-app-only over email/push for the same reason).

### Phased execution plan

**Wave 1 — Security/correctness audit + the one severe pre-existing bug (items 1, 3, 4, 5).**
- Items 3, 4, 5: **no action needed** — confirmed solid by direct code read. Close these out, don't invent work.
- Item 1: fix the fake-onboarding-data problem. Two parts: (a) stop treating fabricated/AI-guessed data as real, permanent rows — at minimum, give exams/homework/timetable entries real delete/edit UI (the missing piece that turns "fake and stuck forever" into "fake and correctable"), and/or clearly mark AI-seeded items as suggestions the student can accept/discard rather than silently-real data; (b) fold in item 14's "add the day's lessons" (timetable) gap here since it's the same root cause. Product should spec the exact UX (delete/edit affordance vs. a "starter suggestions, review and confirm" onboarding step) before Dev builds — this is a product decision, not just a CRUD add.
- Also close the small, real gap found in this pass: wire up `error` from `useTableRows` into at least the highest-traffic screens (Home, School, Exams) so a fetch failure is visibly distinct from "you have nothing" — small, mechanical, high-value.

**Wave 2 — Vibe-coded purge (item 7) + landing page real gaps (item 2).**
- Cheap, low-risk, high-value first: emoji-in-headings removal, em-dash copy pass (real and pervasive, per findings above), reconsidering the blanket page-load fade.
- Bigger, real decision needed from the CEO before starting: the purple-to-cyan brand gradient is the core brand identity (37 usages) — changing it is a rebrand-scale decision, not a bug fix. Flagging for explicit CEO sign-off before Product/Dev touch it, given 3 rounds of prior UI work were built around this exact palette.
- Landing page: add a footer with links to Privacy/Terms/FAQ (once those exist, see Wave 3) — small, but blocked on Wave 3 content existing first.

**Wave 3 — Compliance/SEO/content checklist (item 6), split by effort:**
- *Quick, mechanical, no content decisions needed:* `robots.ts`/`sitemap.ts` (Next app-router convention), custom `not-found.tsx` + `error.tsx`, OG/Twitter meta tags + `alternates.canonical` in `layout.tsx`, cookie consent banner (mechanical to add; the *content/legal* question of what it must say is not).
- *Needs real content/decisions the CEO must provide or approve, not something an agent should invent:* Privacy Policy and Terms of Service copy (legal content — an agent should not author binding legal text unreviewed), FAQ copy, and **Google Analytics specifically needs a GA4 property/measurement ID the CEO creates or provides** — this project has no Google Analytics MCP tool to create one automatically, and per the "vibe-coded" findings above, the literal "load GA before the cookie banner" instruction needs a consent-mode adjustment, not literal compliance, given the minors-as-users consideration already on record in this project.
- Password reset and account deletion belong here too, sized as real, non-trivial features (password reset touches the same `AuthProvider`/session surface that took 4 QA rounds to get right for OTP signup — budget accordingly, don't rush it).

**Wave 4 — UX gaps (items 9, 10, 11, 12, 13, 16) — smaller, contained, mostly additive:**
- Item 13 (homework gets its own sub-nav tab + page, matching Exams' precedent) and item 12 (add user-rename UI next to the already-shipped AI auto-naming) are the smallest, most mechanical of this wave.
- Item 10 (home-screen install instructions, desktop-hidden) is small and has zero infra risk (the PWA plumbing already works).
- Item 16: the fix that actually matters here is a lightweight "achievement unlocked" moment (toast/banner), not touching the award logic, which isn't broken.
- Item 11 (roadmap) needs a real product decision on what actually advances a level — right now nothing does, so this is closer to "design a real mechanism" than "fix a bug."
- Item 9 (tutorial) is the most open-ended of this wave — Product should scope it (a short guided-tour overlay vs. a one-time interstitial) before Dev builds.
- Item 8 (age question) is blocked on the CEO decision flagged above (does it gate anything) before it's built.

**Wave 5 — Larger new builds (items 14's mock-exam-from-past-paper, 15, 17) — sized honestly, each needs its own Product spec pass, not scoped in detail here:**
- Item 15 (notes): **smallest of the three, because it's mostly a reframing/promotion of the existing `study_materials` feature**, not new infrastructure — Product should decide whether to just relabel/surface it better or add a genuinely flat (non-subject-scoped) entry point.
- Item 14 (mock exam from an uploaded past paper): medium — reuses the existing upload pipeline and exam-taking UI, but needs a real new AI generation route grounded in the uploaded material's actual content, which doesn't exist today.
- Item 17 (notifications): the largest of the three, and **blocked on a CEO decision** (in-app-only, vs. extending Resend into a general transactional-email capability, vs. push — which has zero existing infra and this project has consistently avoided adding new external delivery infra for good reason). Recommend in-app-only as the default answer unless the CEO specifically wants email/push, following the same reasoning the Deadlines initiative already used for the identical question.

### Flagged for explicit CEO decision before the relevant wave starts (not guessed at)
- **Item 7's brand gradient removal** — this is a rebrand-scale call (Wave 2), not a bug fix; 3 rounds of prior UI work are built around the current palette.
- **Item 8's age question** — purely informational/analytics, or does it gate something (minors handling)? (Wave 4)
- **Item 6's Google Analytics** — needs a real GA4 property/measurement ID from the CEO; no MCP tool can create one. Also needs a call on consent-mode vs. the literal "before the cookie banner" instruction, given the minors-as-users consideration on record. (Wave 3)
- **Item 6's Privacy Policy / Terms / FAQ copy** — legal/policy content an agent should not author unreviewed and ship as binding. (Wave 3)
- **Item 17's notification channel** — in-app (cheap, matches existing precedent) vs. email (real new Resend integration) vs. push (no infra exists, historically avoided). (Wave 5)
- **Item 1's fix shape** — delete/edit UI for fabricated items, vs. a "review and confirm AI-suggested starter data" onboarding step, vs. dropping the AI-seeding approach entirely in favor of a genuinely empty start. Product should spec this before Dev builds, but the CEO should weigh in on the general direction given how central onboarding's "feels alive from screen one" design goal was to begin with (per `demoData.ts`'s own code comment).

---

PROJECT: Alxioum (formerly FutureOS) — student/founder productivity app. Live production app, not greenfield.

GOAL: New-feature initiative. CEO brief this round: "make the agents work on a new feature they think is good" — no feature pre-decided by the CEO; the team picks it. This is a new initiative layered on top of the UI-improvement initiative below (which is complete and shipped) — that section is kept as history, not overwritten.

**Cato's feature pick for this initiative: Deadlines — a unified, cross-track upcoming-deadlines view (exams, homework, business milestones), free-tier, in-app only.** Full reasoning below. This is the CTO brief handed to Product next; Product should treat this as their spec input the same way they treated Cato's UI audit last time (see PRODUCT_SPECS.md for that precedent — Product should write a `PRODUCT_SPECS_DEADLINES.md` or equivalent, not overwrite the old one).

### Why this feature (Cato's reasoning)

**The gap:** Three different tables already carry due-date semantics — `exams.exam_date`, `homework.due_date`, `business_milestones.due_date` — but the product only ever surfaces the single *soonest* item, on Home, with no visual urgency treatment and no way to see the full list anywhere. Worse: `business_milestones.due_date` is a column that has existed in the schema since the business track shipped and is **never read or written by any UI in the codebase** — confirmed by reading `BusinessPlanHome.tsx` in full and grepping the repo. The add-milestone form only ever inserts `title`/`status`/`order_index`; there is no due-date input anywhere, so the column is structurally dead. That's not a hypothetical gap, it's a shipped half-feature.

Today, a student with 3 overdue homework items and an exam in 2 days sees exactly the same Home screen as a student with nothing due — only the single nearest exam and single nearest homework item render, both in the same neutral gray text as everything else on the page, "Passed" (overdue) included. A founder with 5 stale, past-due milestones has literally no way to know it — the column to track that isn't even wired to an input.

**Who it serves:** every user on both tracks, every day — this is the single most-checked kind of information in a productivity app ("what's due, what's late") and today the app under-serves it despite already storing the data. It's also low-friction to notice as missing, which makes it a credible next feature rather than a nice-to-have nobody asked for.

**Why now:** it directly follows the just-shipped UI initiative (EmptyState system, urgency-capable Badge tones `success`/`warning`/`danger` already exist in `globals.css`/`Badge.tsx`, `formatCountdown()` in `src/lib/utils.ts` already returns `"Passed"`/`"Today"`/`"Tomorrow"`/`"N days"`) — the primitives this feature needs are already in place from that pass, so build cost is low and consistency risk is near zero.

**Why this over the other candidates I weighed:**
- *Edit `business_idea`/`stage` after onboarding* — real gap, but single-field, single-track, low breadth. More a settings fix than a feature.
- *Data export (CSV) for founders' metrics/expenses* — real but niche (business-track only, power-user).
- *Coach conversation search/export* — real but narrow; thread history is already reachable via the existing thread popover, search is a nice-to-have on top of a working feature, not a missing one.
- *Achievements leaderboard* — **rejected, not just deprioritized.** A cross-user leaderboard breaks the app's entire data model: every table today is single-user RLS (`using (user_id = auth.uid())`), and a leaderboard requires an aggregate, other-users-visible query — a genuinely new RLS pattern, not a UI addition. It also raises an unaddressed privacy question for a student app whose users may be minors (surfacing names/ranks publicly). This is a multi-week, product-and-legal-reviewed initiative, not a one-pass feature — correctly out of scope per the brief's own sizing guidance.
- *Student study-time logging outside a formal session* — real but student-only and narrower in daily-use value than deadlines, which every user has by definition.

Deadlines wins on user value (both tracks, daily-relevant), architecture fit (reuses existing tables/hooks/design tokens, zero new RLS patterns, zero migrations), and scope (one pass, not a phase).

### Explicit non-goals for this pass
- **No push notifications, no email, no cron.** The environment has no email-sending infra (no Resend/SES in `package.json`) and Render `autoDeploy`/background jobs are already flagged as unreliable in this project's history — building a delivery pipeline is a separate, materially larger initiative (new provider, new secrets, new failure modes) and isn't what makes this gap painful today. In-app surfacing is the actual fix; "notifications" in the CEO's own gap list is being interpreted as in-app for this pass, not push/email.
- **No new database tables or columns.** `exams.exam_date`, `homework.due_date`, and `business_milestones.due_date` already exist and are sufficient. The only data-layer change is making the *existing, unused* `business_milestones.due_date` column actually settable from the UI (a form field addition, not a migration).
- **No new nav tab.** The app has exactly 5 nav tabs on both tracks (`src/lib/navTabs.ts`, `STUDENT_TABS`/`BUSINESS_TABS`, mirrored 1:1) — adding a 6th competes for scarce mobile bottom-nav space for a feature that doesn't need its own primary destination. Follow the existing precedent of `/app/weekly-review` and `/app/upgrade`: real routes, reachable by a link, not in the tab bar.
- **No editing of milestone due dates after creation** in this pass (set at creation only) — an edit affordance is a legitimate follow-up, not required for the core gap.
- **Not Plus-gated.** Every `PaywallGate`-wrapped route today is AI-generation-heavy (Study subsystem: subjects/quizzes/flashcards/progress/weak-topics, and Coach — confirmed via grep of all `PaywallGate` usages). Exams, homework, and Plan milestones are all free-tier CRUD today. Deadlines is a read-only lens over data users already own for free; gating it would be inconsistent with that pattern and would paywall core organizational value, not incremental AI value.
- **No redesign of Home.** The just-shipped, QA-signed-off `StudentHome.tsx`/`BusinessHome.tsx` are not being reopened for a layout rework. At most, Product may spec a small, additive touch (e.g. urgency-tinting the existing single-item tiles, or a "View all" link) — not a restructure.

### Rough shape (Product to turn into a full spec — this is not one)
- **New route:** `/app/deadlines` (or similarly named) — a single page, not a sub-app, listing every open item with a due date across the user's track, grouped by urgency (Overdue / Today / This week / Later), using the existing `Badge` `danger`/`warning`/`success` tones and `formatCountdown()`.
  - Student: merges `exams` (all) + `homework` (`status = 'pending'`) via the already-existing `useExams`/`useHomework` hooks — no new query, no new API route.
  - Business: `business_milestones` where `due_date is not null and status != 'done'`, via the already-existing `useBusinessMilestones` hook.
- **One small form change:** `BusinessPlanHome.tsx`'s add-milestone form gets an optional due-date input, and the insert call starts writing `due_date`. Each milestone card gets a due-date badge when set (reusing `formatCountdown`).
- **Minimal Home touch:** a "View all" link from the existing Home tiles into `/app/deadlines`, and/or urgency-tinting the existing single-item text when overdue/due-soon — Product's call on exact placement, but no structural Home change.
- **No new API routes, no new Supabase tables/migrations, no new AI calls.** This is a pure client-side aggregation + one form-field addition + one new page.

Pipeline from here: Product writes the full spec next (copy, exact layout, grouping thresholds, urgency-badge rules, the Home touch-point design) → Dev builds → QA reviews → Cato signs off, same as the UI initiative.

CURRENT TASK (Deadlines initiative): ✅ COMPLETE — Cato final sign-off given. Independently re-read the full diff (`git show eeedc5f`), re-verified the grid-safety invariant by grep, booted the dev server and hit `/`, `/app/deadlines`, `/login` directly (all 200, clean compile, no runtime errors), then re-ran `npx tsc --noEmit` and `npx next build` independently (both clean, all 42 routes including `/app/deadlines`) before push. Pushed eeedc5f + 0a10fdd to `claude/futureos-student-app-3ewdz6` and triggered a Render deploy. See QA REVIEW (Deadlines initiative) below for the full adversarial pass this was signed off on.

Spec summary: one new shared lib `src/lib/deadlines.ts` (union `DeadlineItem` type + urgency bucketing/grouping, shared by all 3 touch-points so date math isn't duplicated); one new route `src/app/app/deadlines/page.tsx` (no local layout, no back button — same shape as `weekly-review/page.tsx`); one form addition to `BusinessPlanHome.tsx` (optional native `<input type="date">` for milestone due date, second row under the title input, fully optional/no validation, plus a due-date badge on milestone cards); Home touch-points on both `StudentHome.tsx` (urgency-tints the existing exam/homework tile countdown text + adds a "View all deadlines" button) and `BusinessHome.tsx` (adds due-date badges to milestone cards + same "View all deadlines" button) — both changes append *inside* existing grid cells only, no new `lg:col-start`/`lg:row-start` assignment anywhere, specifically to avoid the CSS Grid subtlety QA caught last pass.

Two deliberate deviations from Cato's rough shape, called out explicitly in the spec's "What I decided / cut" section (not silent): (1) exams are filtered to `exam_date >= today` rather than "all," since a past exam has no genuine "overdue" state (you can't retroactively take it) — homework/milestones keep real overdue semantics since those can still be completed late; (2) business gets **two** distinct empty states (no milestones at all vs. milestones exist but none are dated) rather than one, because every existing milestone in production has `due_date = null` today — collapsing this into one empty state would tell a founder with real active milestones "nothing here," which is false. Cut for this pass: any in-place action on the Deadlines page itself (tap-through/read-only only — exams have no completion concept, so a partial inline-action list would be inconsistent), filter/sort controls (too little data at this stage to justify), drag-to-reschedule/snooze (no due-date editing after creation per Cato's brief, no notification infra to snooze against), and a redundant "Deadlines" section heading on Home (the button copy already carries it).

QA REVIEW (Deadlines initiative, commit `eeedc5f`, reviewed against `DEADLINES_SPEC.md` and Cato's original brief):
- Read `git show eeedc5f` in full (6 files, +335/-57) — not sampled. Cross-referenced every touched file against the spec's exact diffs line by line, not just the commit message's claims.
- `src/lib/deadlines.ts`: byte-for-byte matches spec §0 (`bucketForDate`, `badgeToneForBucket`, `groupDeadlines`, `BUCKET_ORDER`/`BUCKET_LABEL`). Cross-checked `daysBetween`/`todayISO`/`formatCountdown` in `src/lib/utils.ts` for consistency — `formatCountdown`'s "Passed"/"Today" thresholds line up exactly with `bucketForDate`'s overdue/today buckets, no drift between the two.
- Grid regression class (the single most-repeated bug here): grepped `col-start`/`row-start` in `StudentHome.tsx` and `BusinessHome.tsx` — every grid item in both files has both, paired, unchanged by this diff. Confirmed the new "View all deadlines" Button and urgency badges landed *inside* existing `<section>`/grid-cell wrappers (StudentHome's exam/homework `grid grid-cols-2` block got wrapped in an outer non-grid `<div>` inside its existing `lg:col-start-1 lg:row-start-2` section — the section itself didn't move or duplicate; BusinessHome's button sits directly before the existing section's closing tag, still inside `lg:col-start-1 lg:row-start-1`, and renders unconditionally outside the `{!doNext ? ... : ...}` branch as spec required). `BusinessPlanHome.tsx` doesn't use the grid pattern at all (grepped, zero `col-start` hits) so it carries zero regression risk of this class.
- Rendered a real-classNames Playwright harness (Chromium at `/opt/pw-browsers/chromium`, actual compiled `out2.css` from this repo's own `tailwind.config.ts`/`globals.css`, not approximated styles) covering: full `StudentHome`/`BusinessHome` grids with mission/primaryCareer/business_idea/doNext present and absent (390px + 1280px, dark + light) — no misaligned or wrong-row cells in any combination, missing conditional cards leave an expected blank gap in their own pinned row only; the new Deadlines page's 3 distinct empty states (business zero milestones / business milestones-none-dated / student nothing due) and a 4-bucket mixed-urgency list (Overdue/Today/This week/Later, all 4 badge tones); the `BusinessPlanHome` stacked add-milestone form and a due-date-badged milestone card at 390px; the new "View all deadlines" button + urgency-tinted countdown text on both Home screens at 390px and 1280px. All render correctly in both themes, no overflow/truncation/wrap issues, urgency tones map correctly (danger for overdue/today, warning for this-week, success for later).
- Empty-state logic in `deadlines/page.tsx` traced line by line: `hasAnySourceData = isBusiness ? milestones.length > 0 : exams.length > 0 || homework.length > 0` correctly distinguishes "zero milestones at all" (→ "No milestones yet") from "milestones exist, none produced a `DeadlineItem`" (→ "No milestones with a due date"), independent of *why* the list came up empty (no due date set, or all dated ones are `done`) — matches spec §3's documented intent for both sub-cases explicitly.
- Exam filtering `daysBetween(today, e.exam_date) >= 0` confirmed correct: `diff === 0` (exam today) included, `diff < 0` (already passed) excluded — matches spec's stated reasoning that a passed exam has no "overdue" state, and is consistent with the pre-existing identical pattern already used in `src/app/app/school/exams/page.tsx` (`exam.title !== exam.subject` subtitle logic is also copied verbatim from that same file, not a new invented pattern).
- RLS/grants: `business_milestones` has the `_all_own` policy (`for all to authenticated using (user_id = auth.uid()) with check (user_id = auth.uid())`) in `supabase/schema.sql`, and — unlike `profiles` — has no column-restricted `grant update (...)` narrowing what the client can write, so `due_date` was already fully writable before this diff and remains so; no new migration, no new grant statement, nothing to lock down. `profiles` remains the only column-restricted table in the schema (billing columns), untouched by this diff.
- Confirmed no `.upsert()` anywhere in the diff (grepped) — `BusinessPlanHome.tsx`'s milestone insert is a plain `.insert()`, matching the file's pre-existing pattern exactly (same call shape, `due_date: newDueDate || null` appended as the only new field).
- Confirmed `useExams`/`useHomework`/`useBusinessMilestones` all pass through `useTableRows`, which returns `{data: [], loading: false}` immediately when `userId` is `undefined` (read in full) — safe/cheap to call all three unconditionally in the new page. Confirmed the parent `src/app/app/layout.tsx` gates all `/app/*` children (including `/app/deadlines`) behind `loading || !user || !profile`, so `profile.track` is always resolved before `DeadlinesPage` first renders — no flash-of-wrong-track state despite the page branching on `profile?.track` inline rather than via the wrapper-component pattern `/app/page.tsx` and `/app/school/page.tsx` use (a real difference from precedent, but not a bug given the layout-level gate).
- Confirmed `src/lib/navTabs.ts` untouched (no 6th tab) and `TopBar.tsx`'s `activeTab?.label ?? "Overview"` fallback covers `/app/deadlines` exactly as `weekly-review` already relies on.
- `npx tsc --noEmit`: clean (exit 0), run directly.
- `npx next build`: clean (exit 0), run directly — all 42 routes, including `/app/deadlines` at 6.31 kB.
- Verdict: no bugs found. This was a real adversarial pass — full diff read, spec cross-referenced line-by-line against actual shipped code (not the spec's own proposed diff text), RLS/grants checked against `schema.sql` directly, own tsc/build run, own Playwright renders across both themes/both viewports/multiple conditional-sibling combinations plus all 3 new empty states and a full 4-bucket mixed list. QA sign-off given.

BLOCKERS (Deadlines initiative): None.

NEXT TASK (Deadlines initiative): None open. The feature is implemented, QA-reviewed, independently re-verified, and deployed. One non-bug note QA flagged for future consistency: `deadlines/page.tsx` branches on `profile?.track` inline rather than the wrapper-component pattern used by `/app/page.tsx`/`/app/school/page.tsx` — safe today only because `app/layout.tsx` already gates all `/app/*` children on `profile` being resolved, but worth following the established pattern next time a single-page track branch is added.

DEPLOYMENT STATUS (Deadlines initiative): 🟢 Live at https://alxioum-app.onrender.com — pushed (commit bf27a2a) and deployed. Render MCP connector was intermittently unavailable for the automated trigger; CEO triggered the deploy manually via the Render dashboard and confirmed it live.

---

## UI/UX IMPROVEMENT INITIATIVE (COMPLETE — history, not active)

GOAL: UI/UX improvement initiative on the existing live app. CEO brief: "Use the team to make Alxioum better, use Product to make the UI a lot better — fix it." Building is authorized now, not just planning. Scope: visual polish, consistency, and UX gaps in the existing screens — not new features, not a redesign of the architecture.

MVP: N/A — post-MVP, live app with paying users on two locked tracks (student, business).

FEATURES: Student track: Home, School (Subjects/Exams/Flashcards/Quizzes/Progress study system), Future (career matches + roadmap), Coach (AI chat), Profile (XP/achievements/skills). Business track: Home, Plan (milestones), Grow (metrics/expenses/content helper/competitors), Coach, Profile. Shared: onboarding + /choose-plan (track lock), Stripe billing + PaywallGate, missions, weekly review.

TECH STACK: Next.js 15 (App Router) + TypeScript + Tailwind. Supabase (Postgres + Auth). Stripe. Anthropic Claude API. Hosted on Render (https://alxioum-app.onrender.com). Design tokens via CSS custom properties in globals.css (dark-first, `.light` override class), gradient/glass utility classes, RadialStat/StatTile/Card/Badge/ProgressBar as the shared visual primitives.

ARCHITECTURE: Two product tracks (student, business) chosen once at /choose-plan and permanently locked by a Postgres trigger once onboarding_completed is true. Route-level branching pattern: shared routes (`/app`, `/app/school`, `/app/future`) render a track-specific component (e.g. `StudentHome`/`BusinessHome`, `StudentSchoolHome`/`BusinessPlanHome`, `StudentFutureHome`/`BusinessGrowHome`) chosen by `profile.track` in a thin page/layout wrapper. Coach and Profile are track-agnostic single implementations with inline conditional copy. Nav (SidebarNav desktop, BottomNav mobile) is driven by one shared `STUDENT_TABS`/`BUSINESS_TABS` table in `src/lib/navTabs.ts`. The student School tab has a real sub-app (SchoolSubNav: Home/Subjects/Exams/Flashcards/Quizzes/Progress); the business Plan tab is intentionally a single flat page with no sub-nav (by design, per code comment in `school/layout.tsx`) — this is the root cause of most business-track UI thinness identified below.

CURRENT TASK: ✅ COMPLETE — Cato final sign-off given. Re-ran `npx tsc --noEmit` and `npx next build` independently (both clean, all 41 routes) before push. Pushed cc8c4ed + 4286c07 to `claude/futureos-student-app-3ewdz6` and triggered a Render deploy. See QA REVIEW below for the full adversarial pass this was signed off on.

QA REVIEW (this pass, commit cc8c4ed reviewed against 91ac1e0):
- Read `git show cc8c4ed` in full (18 files, +226/-126) — not sampled.
- EmptyState adoption: spot-checked every one of the ~19-20 sites against PRODUCT_SPECS.md's table (copy, icon, CTA, bare/wrapped mode) — all match verbatim, including the deliberately-varied ones (exams: no CTA, correctly — the page genuinely has no add-exam control anywhere, confirmed by reading the whole file; weekly-review: Dev used `bare` mode though the spec row didn't say so explicitly — correct call, since that empty state is nested inside an existing Card/CardContent with a "Skills" heading above it, exactly the case `bare` exists for).
- Confirmed via grep that all 5 explicitly-excluded sites (StudentHome mini stat tiles, Coach thread popover, CountrySelect, quiz-results inline fallback, weak-topics inline text) are byte-for-byte untouched.
- Achievements: exactly the 5 named keys (`first_study_session`, `study_sessions_10`, `first_quiz`, `first_mock_exam`, `flashcards_mastered_20`) got `track: "student"`, the other 4 untouched; filter `ACHIEVEMENTS.filter((a) => !a.track || a.track === profile.track)` applied correctly in profile/page.tsx, `profile.track` confirmed in scope and guarded by the existing `if (!profile) return null`.
- CSS Grid bug class (the recurring `col-start`-without-`row-start` bug): grepped every `col-start` in StudentHome.tsx, BusinessHome.tsx, BusinessGrowHome.tsx, BusinessPlanHome.tsx — every grid item in both StudentHome and BusinessHome has both `col-start` and `row-start` explicitly set (BusinessGrowHome/BusinessPlanHome don't use the grid pattern at all). The diff didn't touch any grid-positioning classNames, only cell content. Built a static Tailwind-compiled render harness (real globals.css + tailwind.config, real classNames) and screenshotted StudentHome and BusinessHome at 390px/1280px, light/dark, with mission/primaryCareer/business_idea conditionally omitted (fresh-account scenarios) — no misaligned or wrong-row items in any combination; missing conditional cards leave an expected blank gap in their own pinned row, nothing shifts.
- No `.upsert()` against `profiles` anywhere in the diff (grepped) — the only `profiles`-adjacent upsert in the repo is pre-existing and against `business_profiles`, untouched by this task.
- No Supabase/migration files touched by this diff — track-lock/RLS/entitlement grants are out of scope for this pass and unaffected.
- Confirmed the pre-existing dead `src/components/ui/EmptyState.tsx` is genuinely unimported anywhere (grepped) — Dev's flag was accurate, left alone as agreed.
- `npx tsc --noEmit`: clean (exit 0), run directly, not taken on Dev's word.
- `npx next build`: clean (exit 0, all 41 routes compiled/prerendered), run directly.
- Mobile-safety check: Coach's only change is `lg:mx-auto lg:w-full lg:max-w-2xl` (confirmed single-line diff, no unprefixed touch). BusinessHome's RadialStat enlargement and milestones caption live entirely inside the `hidden ... lg:block` desktop-only hero card — zero mobile impact. BusinessPlanHome's `border-accent/30` and BusinessGrowHome's unprefixed Badge/"Recent" labels are intentionally cross-viewport (those pages have no separate mobile/desktop split to begin with) — rendered both at 390px via harness, no overflow/wrap/breakage.
- Verdict: no bugs found. This was a real adversarial pass, not a rubber stamp — full diff read, spec cross-referenced line-by-line, exclusions grepped, own tsc/build run, own Playwright renders across both themes/both viewports/multiple conditional-sibling combinations. QA sign-off given.

Full specs: see PRODUCT_SPECS.md at repo root.

COMPLETED TASKS: Full Alxioum build history (onboarding, School study system, Future/career matching, AI Coach, Profile, Stripe billing, student/business track split) — see prior task list, all marked complete. This audit is the first task of the new UI-improvement initiative.

BUGS: None found — QA ran a full adversarial pass on cc8c4ed (diff read in full, spec cross-referenced site-by-site, exclusions grepped, own tsc/build run, own Playwright renders) and found zero regressions. Notable code-quality/product issues (not blocking, not new — carried from Cato's original audit):
- ~~Profile page achievements grid ... not track-aware~~ FIXED this pass (Priority 2).
- ~~No shared `EmptyState` component exists~~ FIXED this pass (Priority 1) — new component at `src/components/shared/EmptyState.tsx`.
- Business track (Plan + Grow) is visually and structurally flatter than the student track by design/history, not oversight — partially addressed this pass (Priorities 3-5: hero ring parity, Grow trend badge/Recent labels, Plan hero accent border); Product explicitly decided against building a business sub-nav, so the deeper structural asymmetry remains by design, not oversight.
- Dev finding, not fixed this pass (out of spec scope): there is a pre-existing, unused `src/components/ui/EmptyState.tsx` with a different prop shape (`body`/`action` instead of `subtitle`/`cta`, no `bare` mode) that is not imported anywhere in the codebase. It now sits alongside the new `src/components/shared/EmptyState.tsx` this pass built per spec. Worth a follow-up to delete the dead `ui/EmptyState.tsx` so it doesn't get picked up by mistake later — flagging for Cato/QA rather than deleting it myself since PRODUCT_SPECS.md didn't mention it.

BLOCKERS: None. CEO has authorized building.

NEXT TASK: None open on this initiative. All 6 UI/UX priorities from PRODUCT_SPECS.md are implemented, QA-reviewed, and deployed. Remaining known gaps are intentionally deferred (see BUGS): the dead `ui/EmptyState.tsx` cleanup, and the business Plan/Grow structural asymmetry (a deliberate Product decision, not a bug).

DEPLOYMENT STATUS: 🟡 Deploying — pushed to `claude/futureos-student-app-3ewdz6` and triggered on Render; confirming live at https://alxioum-app.onrender.com once the build finishes.

---

## QA LIVE-DATA PASS (production Supabase, Deadlines feature) — 2026-08-27

CEO explicitly authorized this pass: seed real, disposable rows directly in production Supabase (project `bgfhcpegsdyxpvmdxkuc`) via the Supabase MCP connector, verify real behavior, then delete everything and confirm cleanup. Every prior QA pass on this project was code-level/static-render only; this is the first pass with real DB access. Methodology and findings only below — no seeded emails, UUIDs, or secrets recorded, per instruction.

**Method:** created two disposable `auth.users` + `profiles` rows (obviously-fake `@example.invalid` addresses, one per track, `onboarding_completed = true` set directly so the real onboarding/auth flow didn't need to be driven). Read `BusinessPlanHome.tsx`'s actual `addMilestone` insert call and matched its exact column set/values with a plain SQL `insert` (no `.upsert()` anywhere in that path — confirmed again by reading the file, matching the prior code-level QA finding). Seeded 4 exams + 4 homework rows on the student account (past/today/+3d/+20d exams; overdue-pending/today-pending/+5d-pending/completed homework) and 6 milestones on the business account, including the two adversarial cases the spec cares about most: `due_date = null`, and a `status = 'done'` milestone with a past `due_date`.

**Deadlines logic traced by hand against the real rows (not eyeballed — actual SQL run, actual output shown):**
- Student: reproduced `exams.filter(daysBetween(today, exam_date) >= 0)` + `homework.filter(status === 'pending')` as SQL. Result: 6 items — past exam correctly excluded entirely (not even bucketed "overdue", matching the documented, deliberate spec deviation that exams have no overdue state); today's exam+homework both bucketed "today"; +3d exam and +5d homework both bucketed "this-week"; +20d exam bucketed "later"; overdue homework correctly bucketed "overdue"; the `completed`-status homework was correctly excluded regardless of its due date.
- Business: reproduced `milestones.filter(due_date != null && status !== 'done')` as SQL. Result: exactly 4 items (overdue/today/this-week/later, one per bucket, thresholds matching `bucketForDate` exactly: diff -7/0/+4/+14). **The highest-risk case held**: the `done` milestone with a past due date (diff -2, would have bucketed "overdue" if leaked) was correctly excluded, and the null-due-date milestone was correctly excluded. No bug found here — this is the one Product/Dev were most likely to get wrong and it's correct in production.

**RLS — checked live `pg_policies`, not just repo `schema.sql` (no drift found):** `business_milestones_all_own`, `exams_all_own`, `homework_all_own` are all `FOR ALL ... USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid())`; `profiles` has separate `id = auth.uid()` policies for select/insert/update. Went further than a policy-text read: impersonated the disposable student account in-session (`set local role authenticated; set local "request.jwt.claims"`) and ran real queries as that role — confirmed 0 rows of the other account's `business_milestones` visible, 0 rows of the other account's `homework` visible, and a real cross-account `UPDATE` attempt against the business account's milestone affected 0 rows. Also attempted a real self-upgrade (`update profiles set plan = 'plus', plan_status = 'active'` as the impersonated student): rejected with a Postgres `42501 permission denied for table profiles`, confirming the column-restricted `UPDATE` grant is live and enforced, not just present in `schema.sql`. Live `authenticated` UPDATE grant on `profiles` matches the repo's intended column list exactly (billing columns `plan`/`plan_status`/`trial_ends_at`/`stripe_customer_id`/`stripe_subscription_id` excluded).
- Minor hygiene note, not a live vulnerability: `anon` still carries the default Supabase blanket `UPDATE` grant on all `profiles` columns (including billing) — this is standard/default for every table in a fresh Supabase project and is inert today because no `profiles` policy targets `anon` (RLS denies it 0 rows regardless of column grant). Flagged for Cato as defense-in-depth hygiene, not urgent: worth an explicit `revoke update on public.profiles from anon;` so a future policy change can't accidentally combine with this pre-existing grant.

**`get_advisors` (security + performance) — run for the first time on this project:**
- Security (3 WARN): (1)+(2) `public.prevent_track_change_after_onboarding()` (the track-lock trigger function) is `SECURITY DEFINER` and technically callable by `anon`/`authenticated` via `/rest/v1/rpc/...`. **Verified directly, not assumed**: called it as `authenticated` and got `ERROR 0A000: trigger functions can only be called as triggers` — Postgres itself blocks direct invocation outside trigger context, so this is not exploitable today. Still worth a one-line `REVOKE EXECUTE` for hygiene/to silence the advisor, flagged to Cato/Dev as low-priority. (3) Leaked-password protection (HaveIBeenPwned check) is disabled in Auth settings — genuine, real gap, flagged to Cato: cheap to turn on, no downside found.
- Performance (59 findings, none CRITICAL): 34 `auth_rls_initplan` WARNs — every single `_all_own` RLS policy in the schema (including `business_milestones`, `exams`, `homework`, all 3 `profiles` policies) re-evaluates `auth.uid()` per-row instead of `(select auth.uid())`, which is the standard Supabase perf footgun at scale. This is systemic across the whole schema, not specific to the Deadlines feature — flagged to Cato as a backlog item (rewriting every `_all_own` policy), not a Deadlines-blocking bug. 21 `unindexed_foreign_keys` (INFO) including `exams.study_subject_id_fkey`. 4 `unused_index` (INFO).

**Cleanup — verified, not assumed:** deleted all seeded rows (`exams`, `homework`, `business_milestones`, `profiles`, `auth.users`) for both disposable accounts, then ran a follow-up `SELECT` count against every one of those 5 tables filtered by the disposable UUIDs, plus a `LIKE 'qa-test-%@example.invalid'` sweep across `auth.users` to catch anything the UUID filter might have missed. All 6 counts returned `0`. No disposable data remains in production.

**Verdict:** Deadlines' core urgency-bucketing and cross-source filtering logic is correct against real data, including the adversarial `done`-with-past-due-date case. RLS isolation (including write-path and the billing-column lockdown) held up under live, real impersonation attempts, not just a policy-text read. No blocking bugs found. Two non-blocking items handed to Cato: (1) schema-wide `auth_rls_initplan` perf cleanup (systemic, pre-existing, not introduced by Deadlines), (2) leaked-password protection toggle + `SECURITY DEFINER` EXECUTE-revoke hygiene on the track-lock trigger function (verified not currently exploitable).

---

## QA — FULL APP FUNCTIONAL PASS (both tracks) — 2026-08-28

CEO requested a comprehensive logged-in functional test of the whole app, both tracks. Full findings are
in **`QA_FULL_APP_REPORT.md`** at the repo root (not duplicated here — it's long). Short version:

- **Real live/logged-in testing was not possible this session** — a genuine org egress-policy 403 blocks
  this sandbox from reaching `*.supabase.co` at all (confirmed 3 independent ways: Playwright browser,
  direct `curl`, and the proxy's own status log), and no Supabase MCP/DB tool was available as an
  alternative. This is not the same coverage as the 2026-08-27 live pass and I did not present it as such.
- Fell back to a deep code-level trace of every checklist item, plus `tsc`/`build` gates (both clean) and
  static Playwright renders of the pages reachable without a session.
- **4 real findings, all new** (nothing in `src/` changed since `26e9d89`, so none of these are
  regressions — they were simply never surfaced by a prior pass scoped to a single feature):
  1. **XP-farming via toggle** — `toggleHomework`/`toggleMilestone` award XP on every completion, no
     once-only guard (unlike `completeStudySession`, which has one). Low security impact, real product bug.
  2. **Business metrics trend badge** — no secondary sort key on same-day entries of the same metric key;
     Postgres doesn't guarantee tie order on `logged_date` alone. Exactly the "2+ same-key entries" case
     the task asked me to check, and it's broken.
  3. **Weekly Review page isn't track-aware** — shows a permanently-empty "School" card to every
     business-track account. Same bug class already fixed once elsewhere (commit `6420e46`), missed here.
  4. **No add-exam/add-homework UI exists anywhere** (student track) — only onboarding seeds those tables;
     this blocked me from literally completing that checklist item, live or otherwise. Business track's
     equivalent (add-milestone) is fine. Scope question for Cato/Product, not a Dev bug fix.
- Explicitly not re-litigated: track-lock trigger and RLS isolation are carried forward from the
  2026-08-27 live pass on byte-identical schema (high confidence, not fresh evidence this session).
- No cleanup needed — the network block meant no write ever reached the database this session.

**Status: not signed off as comprehensively tested end-to-end.** Bugs 1-3 go to Dev; Bug 4 is a scope
call for Cato. Recommend next pass gets live DB access restored (MCP tool or egress allow-list) before
"whole app tested" can honestly be claimed.

---

## UI PREMIUM VISUAL PASS (Product, isolated worktree — parallel with Dev's codebase audit and QA's full-app test)

CEO brief: make the UI look genuinely premium/professional-caliber ("came from 100,000 people web development/app development"), not incremental tweaks. Mandated scope strategy: tokens first (`globals.css`/`tailwind.config.ts`), then shared primitives, only bespoke screens if the uplift doesn't already cover it. Figma/Canva authorized; Lovable explicitly out of scope (separate hosted app, can't edit this codebase).

**Figma/Canva note (No-BS):** neither tool was actually exposed as a callable tool in this execution session (only Read/Write/Edit/Bash/Glob/Grep/WebSearch were available) despite being authorized. Did not fabricate a Figma file/link. Did the design-system work directly in tokens with real Playwright render-harness verification substituting for a Figma mockup review. No genuine Canva need arose — every icon use is already covered by Lucide.

**What shipped (8 files, tokens + primitives only, no screen restructuring):** `src/app/globals.css` + `tailwind.config.ts` — multi-layer elevation/shadow system replacing single soft-blur shadows, light-mode `--shadow-color` deliberately enriched (light has almost no luminance gap between card and page to lean on), new global `:focus-visible` halo ring (previously **zero** focus states existed anywhere in the app — grepped, confirmed), `.glass` refinement (blur/saturate/border), `text-wrap: balance` on headings. `Card.tsx`/`ProgressBar.tsx`/`RadialStat.tsx`/`StatTile.tsx` — transition-shadow, inset track groove, stronger ring track color, `tabular-nums`. Two 1-line mechanical touches in `StudentHome.tsx`/`BusinessHome.tsx` swapping an existing local `HOVER_LIFT` constant's shadow token (no grid/structural changes — `col-start`/`row-start` grepped identical before/after).

**Real bug caught mid-pass, not just a design choice:** `.glass`'s own `box-shadow` was silently overriding the `shadow-card` Tailwind utility applied alongside it on every `Card` (same CSS property, `.glass` sits later in the compiled stylesheet, so it always won) — the app's card elevation was effectively half-dead before this pass; no shadow-token change would have been visible until this was found and fixed (moved the glass "top-edge highlight" into the shadow tokens themselves via a new `--card-highlight` variable instead of `.glass` owning `box-shadow`).

**Deferred, with reasoning (see `UI_REDESIGN_REPORT.md`):** typography scale consolidation (49 arbitrary `text-[Npx]` usages across ~30 files — real but too broad/low-value for one pass), border-radius scale (large-radius language is consistent and part of existing brand identity, not a bug), any screen-level redesign beyond the two mechanical `HOVER_LIFT` touches (the token+primitive uplift alone visibly fixed the biggest issue — flat, undefined light-mode cards — on every screenshotted screen).

**Verification:** no live Supabase session in this worktree, so built a real-component render harness (not hand-approximated HTML) — bundled the actual `Card`/`Button`/`Badge`/`ProgressBar`/`RadialStat`/`StatTile`/`MissionHeroCard`/`EmptyState`/`ScreenHeader`/real Lucide icons/real `cn`/`deadlines` lib via esbuild with mock data, rendered via `react-dom/server`, compiled through the actual `tailwind.config.ts`/`globals.css`, screenshotted with Playwright/Chromium at 390px/1280px, light/dark, before/after, for Home (both tracks), School/Exams, Deadlines, Coach — same harness methodology QA used in the prior UI initiative. Also verified `:focus-visible` with real keyboard Tab navigation (`document.activeElement` + screenshot), not just visual inspection. Harness lived in a git-ignored `.harness-tmp/` at repo root, deleted before final verification — `git status` confirmed clean apart from the 8 intentional source files. `npx tsc --noEmit` and `npx next build` both clean (all 42 routes) after harness deletion.

**Not done in this pass:** functional/data/route changes (none — pure visual pass, confirmed via `git diff` file list), Dev-side implementation review, QA sign-off (both out of scope for this isolated worktree; Cato reconciles across all three parallel worktrees).

DEPLOYMENT STATUS (this pass): not deployed — isolated worktree, awaiting Cato's reconciliation across the parallel Dev/QA/Product worktrees before merge.

---

## CATO — RECONCILIATION OF 3 PARALLEL WORKTREES (Dev audit, QA full-app pass, Product UI pass) — 2026-08-28

CEO ran Dev, QA, and Product in parallel isolated git worktrees (each on their own branch, own commit, none pushed). This section is the CTO merge/reconcile/sign-off pass across all three.

**Merge:** `worktree-agent-ae952817da8c30ad2` (Dev, `7220b5e`) merged clean, fast-forward. `worktree-agent-aa0d88ff66bd706b7` (QA, `e63c475`) merged clean, no conflict (touched `PROJECT_STATE.md` append-only + new `QA_FULL_APP_REPORT.md`). `worktree-agent-ad241ab4da61709c1` (Product, `061c5e2`) conflicted only on `PROJECT_STATE.md` — both QA's and Product's sections were pure appends at the same insertion point, not overlapping edits to existing text — resolved by keeping both sections in full, separated by a `---` divider, nothing dropped or summarized away. `npx tsc --noEmit` and `npx next build` both clean (42 routes) immediately after the merge, before any further changes.

**What each agent did:**
- **Dev** — full codebase audit (billing/webhook idempotency, RLS/grant drift, error handling across all 15 API routes, dead-code sweep). Found and fixed one real issue: deleted `src/components/ui/EmptyState.tsx`, a dead duplicate of `src/components/shared/EmptyState.tsx` with a different, riskier prop shape. Flagged 6 additional unused-but-not-duplicate UI primitives for a judgment call (see below). Full detail in `DEV_AUDIT_REPORT.md`.
- **QA** — attempted a full logged-in functional pass, both tracks; the sandbox's org egress policy genuinely blocked all `*.supabase.co` traffic this session (confirmed 3 independent ways: Playwright, direct `curl`, proxy status log), so no live/DB-backed testing was possible. Fell back to a deep code-level trace of the entire checklist and found 4 real, pre-existing bugs (not regressions — verified `src/` was byte-identical to the last-reviewed commit before starting). Was explicit throughout that this pass is not equivalent to the 2026-08-27 live pass and did not claim otherwise. Full detail in `QA_FULL_APP_REPORT.md`.
- **Product** — premium visual uplift, tokens + shared primitives only (`globals.css`, `tailwind.config.ts`, `Card`/`ProgressBar`/`RadialStat`/`StatTile`, one 1-line shadow-token swap each in `StudentHome.tsx`/`BusinessHome.tsx`), no screen restructuring. Along the way found and fixed a real, previously-shipped bug: `.glass`'s own `box-shadow` was silently overriding the `shadow-card` Tailwind utility on every `Card` (same CSS property, later in cascade) — the app's card elevation system had been half-dead since it shipped. Honest that Figma/Canva weren't actually exposed as callable tools this session despite being authorized — did not fabricate a Figma file, did the token work directly with real Playwright before/after verification instead. Full detail in `UI_REDESIGN_REPORT.md`.

**Cato's disposition of QA's 4 findings:**

1. **XP-farming (`toggleHomework`/`toggleMilestone`, no once-only guard) — fixed.** Applied the same one-way-completion guard `completeStudySession` already uses: both functions now early-return once the item is already completed/done, and the "mark as pending"/"mark as not done" undo affordance is removed (button disabled once complete, matching `completeStudySession`'s exact UX). This is a deliberate call to make completion semantics consistent across all three XP-awarding actions in the app rather than adding a new `xp_awarded_at` column to preserve toggle-back — zero migration, zero new failure surface, closes the exploit completely. Files: `src/app/app/school/StudentSchoolHome.tsx`, `src/app/app/school/BusinessPlanHome.tsx`.
2. **Business metrics trend badge (no tiebreaker on same-day entries) — fixed.** Extended `useTableRows`'s `orderBy` option to accept an array of clauses (backward compatible, single-clause callers unchanged), and added `created_at desc` as a secondary sort key to `useBusinessMetrics`. Same-day same-key entries now resolve deterministically to insertion order. Files: `src/lib/hooks/useTableRows.ts`, `src/lib/hooks/domain.ts`.
3. **Weekly Review not track-aware — fixed.** `weekly-review/page.tsx` now branches on `profile.track`, following the established Home/School/Future pattern (see `6420e46` precedent). Business accounts get a "Plan" card (milestones completed all-time, expenses logged this week — added `useBusinessExpenses` to the page) in place of the "School" card, and `nextFocus` now generates milestone-based copy for business instead of exam/homework language that could never fire for that track. `weekly_reviews` has no business-specific columns, so the existing generic int columns (`assignments_completed`, `study_minutes`) are reused per-track (documented inline) rather than adding new columns — no migration. File: `src/app/app/weekly-review/page.tsx`.
4. **No add-exam/add-homework UI — fixed with a minimal addition, not deferred.** Judgment call: this is genuinely small to close (both `exams/page.tsx` and `StudentSchoolHome.tsx`'s homework list already had `EmptyState` copy implying an add flow that was never wired up), and the cost of leaving it unfixed is severe — every real student's exams/homework (and, by extension, the Deadlines feature) permanently freezes at whatever onboarding seeded, for the lifetime of the account, with zero way to add more. Added an inline add-exam form (subject + required date, matching `BusinessPlanHome`'s add-milestone form styling) to `src/app/app/school/exams/page.tsx`, and an inline add-homework form (subject + title + required due date, priority defaults to `medium`) to the Homework section of `src/app/app/school/StudentSchoolHome.tsx`. Both are plain `.insert()` calls, no new tables/columns, same pattern `BusinessPlanHome.addMilestone` already established. Business track already had this (add-milestone), so this closes the last remaining track asymmetry of this class.

**Cato's call on Dev's 6 flagged files — kept, not deleted.** `FadeIn.tsx`, `Modal.tsx`, `Switch.tsx`, `Tabs.tsx`, `Tooltip.tsx`, `Avatar.tsx` (`src/components/ui/`) are unimported anywhere but, unlike the deleted `EmptyState.tsx` duplicate, aren't a duplicate of an in-use component with a divergent prop shape — there's no risk of a future dev importing the wrong one by mistake, and they read as a normal standalone design-system surface built ahead of a feature that would need a modal/tabs/tooltip/switch/avatar. Deleting working, harmless, unused code on spec here would be speculative pruning, not a bug fix — agreeing with Dev's own recommendation to leave them alone.

**Final verification:** `npx tsc --noEmit` clean (exit 0) and `npx next build` clean (exit 0, all 42 routes, including `/app/deadlines`, `/app/weekly-review`, `/app/school/exams`) after all 4 bug fixes, run directly by Cato — not taken on any agent's word.

**Not re-litigated / carried forward:** the track-lock trigger and RLS isolation (verified live by QA on 2026-08-27, byte-identical schema since, not re-attacked this pass since no schema/policy files changed). The two non-blocking hygiene items from the 2026-08-27 live pass (schema-wide `auth_rls_initplan` perf cleanup, leaked-password-protection toggle + `SECURITY DEFINER` EXECUTE-revoke) remain open backlog items, not addressed this pass — neither is urgent (both already flagged as non-blocking) and neither is in scope of reconciling these 3 worktrees.

BLOCKERS: None.

NEXT TASK: None open from this reconciliation. Recommend the next QA pass gets live Supabase access restored (MCP tool or egress allow-list) so the "full app, both tracks, logged in" functional test QA attempted this round can actually be completed live — QA was explicit that this round's code-level trace, while it found 3 real bugs (now fixed) plus the scope gap (also now fixed), is not equivalent coverage to a live pass.

DEPLOYMENT STATUS (this reconciliation): not deployed. Merged and fixed locally on `claude/futureos-student-app-3ewdz6`, committed, not pushed — CEO handles push + Render deploy trigger after reviewing this report, per standing process.

---

## UI PREMIUM VISUAL PASS — ROUND 2 (Product, worktree `agent-a4979b1aa14970469`)

Picked up the two things round 1 explicitly deferred (typography-scale consolidation, screen-level polish on the newest UI) plus a re-check of Figma/Canva availability. Full detail in `UI_REDESIGN_REPORT.md` (round 2 section, appended). Short version:

- **Figma/Canva: still not exposed as callable tools this session** (same honest finding as round 1) — proceeded in-code with Playwright verification, no fabrication.
- **Typography scale: consolidated, not just deferred.** Re-audited the 51 arbitrary `text-[Npx]` usages (30 files) and found they were already a de facto, repeated scale (11px alone was 22 of 51, always the same "eyebrow" role) rather than true noise — so instead of a risky broad rewrite, added 9 named `fontSize` tokens to `tailwind.config.ts` (`2xs`/`caption`/`tooltip`/`label`/`body`/`title`/`title-lg`/`heading`/`display`) that are byte-for-byte the same pixel values, and mechanically renamed every usage to the matching token. Zero visual diff, zero regression risk (renamed, not resized) — confirmed via `grep` returning zero remaining `text-[Npx]` matches and matching per-token counts. One value (14px) needed no new token since it was already identical to Tailwind's own default `text-sm`.
- **Real regression caught and fixed: round 1's `.glass`/`shadow-card` fix left 25 `glass` elements across 5 files with zero box-shadow.** Round 1 correctly moved `box-shadow` out of `.glass` and into paired `shadow-*` utilities, but never audited every existing `glass` call site for whether it actually had one — it didn't, in 25 places: the entire Coach page, the subject-session AI tutor page (14 separate blocks), the homework-help chat page, and 2 onboarding result cards. This was invisible in round 1 because that pass's screenshots covered Home/Deadlines/School-Exams, not Coach — caught this round specifically because Coach was in scope. Fixed by adding a role-appropriate `shadow-subtle`/`shadow-card`/`shadow-raised` to all 25 sites.
- **Screen-level polish on the two brand-new forms from the last reconciliation** (`StudentSchoolHome.tsx` add-homework, `exams/page.tsx` add-exam — both added by Cato to close QA's "no add-exam/homework UI" finding, neither had a design pass). Found a real mobile bug: the homework form crammed 4 elements (subject/title/date/submit) into one flex row, leaving the title input roughly 40-50px wide at 390px — broken, not just tight. Restructured both to two-row layouts matching the visual grammar `BusinessPlanHome`'s existing add-milestone form already established (max one `flex-1` per row). No new components, no functional changes — pure DOM re-grouping of existing inputs.
- **Considered and explicitly rejected:** urgency-tinting the Deadlines page's source-type icon circles — the urgency signal is already carried by the badge tone and section header; a third color signal would be redundant and would compete with the icon's existing "item type" role. Left unchanged.
- **Verification:** same real-component Playwright-harness methodology as round 1 (esbuild-bundled real components + hand-copied verbatim classNames, compiled through the actual `tailwind.config.ts`/`globals.css` via the Tailwind CLI, screenshotted at 390px/1280px × light/dark). Confirmed Coach's cards/bubbles/panel now read as lifted in both themes (previously flat), both redesigned forms have comfortable input widths at 390px, and the new typography scale renders as a coherent hierarchy with no size regressions. Harness lived in git-ignored `.harness-tmp/`, deleted before commit.
- **Build gates:** `npx tsc --noEmit` clean, `npx next build` clean (all 42 routes), both run directly after harness deletion.
- **Grid safety / mobile / both themes / no functional changes:** all re-confirmed the same way as round 1 (see `UI_REDESIGN_REPORT.md` for the full checklist) — `col-start`/`row-start` pairing on `StudentHome.tsx`/`BusinessHome.tsx` untouched by this round's edits.

Files changed (round 2): `tailwind.config.ts` (new fontSize scale) + 33 `.tsx` files (typography-token rename, `shadow-*` additions to previously shadow-less `glass` elements, and the two form restructures). Full list via `git diff --stat` on commit noted below.

BLOCKERS: None.

NEXT TASK: None open from this round. Recommend the next visual pass (if any) look at border-radius scale review and any remaining bespoke-screen polish beyond what two rounds of token/primitive work already covered — both still deferred, same reasoning as round 1 (large-radius language is a deliberate brand choice, not a bug; no further screen found that clears the guiding-question bar without restructuring, which is out of scope per Cato's standing "no Home redesign" direction).

DEPLOYMENT STATUS (round 2): not deployed — isolated worktree, committed locally on `claude/futureos-student-app-3ewdz6` via worktree `agent-a4979b1aa14970469`, not pushed. Awaiting Cato/CEO review and merge, per standing process.

---

## QA — FULL APP FUNCTIONAL PASS, ROUND 2 (both tracks) — 2026-08-28

CEO asked for a second comprehensive logged-in functional test, both tracks, now that `acab9ef` (the
round-1 bug fixes) is live in production. Full findings in **`QA_FULL_APP_REPORT.md`** at repo root
(overwritten this round, round-1 content preserved in git history at commit `e63c475`). Short version:

- **Step 1 retried, still blocked.** Confirmed 3 independent ways again this session (Playwright
  `ERR_TUNNEL_CONNECTION_FAILED` on a real signup attempt, direct `curl` 403, proxy status log
  `connect_rejected` 403 to `bgfhcpegsdyxpvmdxkuc.supabase.co:443`) — same org egress policy denial as
  round 1, not session noise. No Supabase MCP/DB tool was exposed in my available toolset this session
  either, so the direct-seed-and-trace fallback was also unavailable again. Did not fabricate live testing.
- **Went one step further than round 1 anyway**: built a temporary, unauthenticated render-harness route
  (deleted before commit) that served the *exact* JSX/classNames of the new add-exam/add-homework/
  add-milestone forms through the real dev server + real compiled Tailwind, and screenshotted it with
  Playwright at real phone widths (320/375/390px), checking actual `getBoundingClientRect()` geometry, not
  eyeballing.
- **Found a real, severe, newly-shipped bug this way**: the add-exam form (`exams/page.tsx`) and
  add-homework form (`StudentSchoolHome.tsx`) — both added this round to close round 1's Bug 4 — overflow
  their container on real phone widths because their `flex-1` text input is missing `min-w-0` (present on
  every other `flex-1` usage in these same files, just not these two new forms). Combined with this app's
  pre-existing `overflow-x: hidden` on `html`/`body` (confirmed pre-existing via `git blame`, not touched
  this round), the date-picker input and submit button are pushed **entirely outside the viewport with no
  way to scroll to them** — not degraded, completely unreachable. A student on a real phone (320–390px,
  most phones) cannot tap "Add exam" or "Add homework" at all right now, in production. The add-milestone
  form (business track) is unaffected — renders correctly at all tested widths.
- **Second, lower-severity new gap found in the same investigation**: the add-exam date input has no
  `min={today}` guard, and exams have no edit/delete UI anywhere — a mistyped past date becomes a
  permanently-stuck "Passed" exam on both Home and School screens with no way to fix it. Structurally
  unreachable before this round (only onboarding ever wrote to `exams`, always future dates); now
  reachable via the new form.
- **All 4 of round 1's fixes re-verified genuinely correct at the code level** (XP-farming guard, metrics
  trend-badge tiebreaker, track-aware Weekly Review, add-exam/homework forms existing and correctly wired
  at the data layer) — confirmed by reading the actual shipped `acab9ef` code, not the commit message.
  `business_metrics.created_at` and the RLS/grant shape of every touched table confirmed against
  `schema.sql` directly (unchanged by this diff — no new migration needed).
- Grid-regression class (`col-start`/`row-start`) grepped clean across all 4 touched files — none use the
  grid pattern, zero risk of that bug class here. This is a different failure mode (flexbox min-width).
- `npx tsc --noEmit` and `npx next build` both clean, run directly, twice (before/after the harness route
  was added and removed) — harness confirmed fully absent from the final route list and `git status`.
- No live data touched this session (network block prevented any write reaching the database) — no
  cleanup sweep needed, verified via `git status`, not assumed.

**Status: NOT signed off.** Bugs 1–3 from round 1 confirmed fixed. Bug 4 from round 1 is closed in spirit
but its own fix ships a new, severe mobile regression (add-exam/add-homework forms unusable on real phones)
that must go back to Dev before this round can be called done. Root-cause fix is narrow and well-understood
(add `min-w-0` to the `flex-1` title/subject inputs in both new forms — same pattern already used
correctly elsewhere in the same files) — recommend Dev fix + QA re-verify via the same render-harness
technique (real Playwright render at 320/375/390px, not just a JSX read) before Cato re-signs-off. Bundle
the `min={todayISO()}` fix for the past-dated-exam gap into the same patch since it's the same file.

---

## CATO — RECONCILIATION OF 3 PARALLEL ROUND-2 WORKTREES (Dev audit, Product visual pass, QA overflow bug) — 2026-08-28

CEO ran a second round of Dev/Product/QA in parallel isolated worktrees off `acab9ef`. This section is the CTO merge/reconcile/fix/sign-off pass across all three.

**Merge sequence:** Dev (`worktree-agent-a554984a31f5d5ad2`, `26bef55`) merged clean, no conflict (report-only, `DEV_AUDIT_REPORT.md` only). Product (`worktree-agent-a4979b1aa14970469`, `a23c540`) merged clean, no conflict (touched 36 files, but no overlap with Dev's report-only commit). QA (`worktree-agent-a1c15912007ddcf99`, `34204f1`) conflicted only on `PROJECT_STATE.md` — Product's and QA's round-2 sections were both pure appends at the same insertion point, not overlapping edits to the same prose — resolved by keeping both sections in full, separated by a `---` divider, nothing dropped or summarized. `npx tsc --noEmit` and `npx next build` both clean (42 routes) immediately after all three merges, before any further changes (commit `99cf2ca`).

**What each agent did this round:** Dev re-verified round 1's fixes against the pre-fix diffs (not just the after-state) and checked all 17 `useTableRows` callers — zero new bugs, report-only. Product consolidated 51 arbitrary `text-[Npx]` usages into 9 named Tailwind typography tokens (byte-identical values, pure rename, zero visual diff) and — independently, in the same pass — caught and fixed its own round-1 regression: 25 `.glass` call sites across Coach, the AI-tutor chat, homework-help, and onboarding-results that never got a paired `shadow-*` utility after round 1 moved `box-shadow` out of `.glass`, silently going visually flat. Product also reflowed the two brand-new add-exam/add-homework forms (added last round to close QA's round-1 Bug 4) from one cramped row into two, independently of QA's parallel investigation into the same forms. QA re-confirmed all 4 of round 1's fixes hold, again could not reach live Supabase (network-blocked, confirmed 3 ways again), and built an unauthenticated Playwright harness against the real dev server + real compiled CSS at real phone widths (320–390px) — finding a genuine mobile-breaking bug in the same two new forms Product had just touched.

**The overflow bug — verified against the actual merged code, not QA's pre-merge description.** QA's report (written in an isolated worktree, before merge) describes the bug against the *original* single-row, 4-element form layout, where a missing `min-w-0` on the flex-1 title/subject input, combined with 2+ flex-basis-auto siblings each defaulting to `min-width: auto` (browser default ~170-200px per bare `<input>`), summed to far more than the ~340px of available width at phone widths — pushing the date input and submit button off-screen with the app's pre-existing `overflow-x: hidden` on `html`/`body` making them unreachable. By the time both branches were merged, Product's independent two-row reflow (landed in the same round, unrelated motivation — visual polish, not this bug) had already changed the form shape: each row now holds at most one `flex-1` element next to fixed-width siblings, and I confirmed via a real Playwright render harness (esbuild-free, exact copy-pasted JSX/classNames from the actual merged files, served through the real Next.js dev server, real compiled Tailwind, screenshotted at 320/375/390px) that the severe off-screen failure QA found **no longer reproduces** in the merged two-row layout, with or without `min-w-0` — `document.documentElement.scrollWidth` equals `clientWidth` at all three widths either way.

That does not make QA's finding wrong or the fix unnecessary — it means the fix target moved. `min-w-0` was still missing from 4 flex-1 inputs across both forms (title in the homework form's row 1, and the date input in each form's row 2) where every *other* flex-1 element in these same files correctly has it, matching an established codebase invariant, and it's a zero-risk, zero-visual-diff, one-line defensive addition against exactly the failure class QA identified (long unbroken content, a future crowded-row edit, or a narrower device than tested) even though it isn't live-breaking today. Applied `min-w-0` to all 4: `src/app/app/school/exams/page.tsx` (date input, add-exam form) and `src/app/app/school/StudentSchoolHome.tsx` (title input row 1, date input row 2, add-homework form).

**Verification, not assumption:** rendered both forms through a temporary route (`src/app/harness-verify-xyz123/`, JSX copy-pasted verbatim from the real merged source, deleted before commit — confirmed absent via `git status` and a re-run `npx next build` showing all 42 routes with no trace of it) via the real `npm run dev` server. Reproduced the pre-fix state first (removed `min-w-0` from the homework-form title input only, matching QA's literal finding) and confirmed even that narrower repro did not overflow post-reflow (223-244px title width, comfortable) — establishing that Product's reflow, not the `min-w-0` fix, was what closed the severe failure QA hit. Then re-applied the fix and confirmed via Playwright `boundingBox()` on the date input and submit button (all 4 combinations of {exam-form, hw-form} × verified fully within `[0, viewportWidth]`) and `document.documentElement.scrollWidth === clientWidth` at 320/375/390px that there is no horizontal page overflow in the final merged+fixed state. Screenshot confirms both forms render cleanly, all controls visible, no clipping.

**Secondary finding (no `min` date guard on exams, no exam edit/delete) — fixed the cheap part, deferred the rest.** Added `min={todayISO()}` (already imported/available via `src/lib/utils.ts`) to both the exam date input and, for the same reason and same one-line safety, the homework due-date input — a native HTML constraint that blocks picking a past date in the date-picker UI and triggers form-level validation on submit. This is a soft client-side guard only (not a server-side/RLS constraint), which is the right size for what's actually a UX footgun, not a security issue. **Deferring exam edit/delete UI** — real gap (a still-possible bad date, e.g. via manual keyboard entry bypassing the picker, becomes a permanently stuck tile), but it's a materially bigger surface (new UI affordance, new interaction pattern, decisions about undo/confirm) than this round's scope, and homework already has a partial escape hatch (mark-as-completed) that exams structurally lack today by design (exams have no completion concept per the original Deadlines-initiative decision) — building real exam mutation UI is better sized as its own small task than bundled into a bug-fix round.

**Final verification:** `npx tsc --noEmit` clean (exit 0) and `npx next build` clean (exit 0, all 42 routes) after the merge and after the fix, run directly — not taken on any agent's word. `.next` cache was cleared once mid-session after a stale generated-types reference to the deleted harness route surfaced a transient `tsc` error; re-run confirmed clean.

BLOCKERS: None.

NEXT TASK: None open from this round. Recommend exam edit/delete UI as a right-sized standalone task if the CEO wants to close the deferred secondary gap, and the next QA pass still needs live Supabase access restored (unchanged recommendation from round 1's reconciliation — this round's network block was independently re-confirmed 3 ways, same as round 1).

DEPLOYMENT STATUS (this round 2 reconciliation): not deployed. Merged, fixed, and committed locally on `claude/futureos-student-app-3ewdz6` — CEO handles push + Render deploy trigger after reviewing this report, per standing process.

---

## UI PREMIUM VISUAL PASS — ROUND 3 (Product, worktree `agent-a75f9963c88db1c5d`, full tool access) — 2026-08-29

**Trigger:** rounds 1–2 shipped real token/shadow/typography/focus-state work, but a real human tester's verdict came back unchanged from before any of it: *"the ui looks vibe coded and just looks bad."* This round had full tool access (Figma, Canva, unrestricted WebSearch) — deliberately different from rounds 1–2, which ran as the constrained `product` subagent (Read/Write/Edit/Bash/Glob/Grep/WebSearch only) despite Figma/Canva being nominally authorized both times.

**Diagnosis (screenshotted, not assumed):** the problem was never the tokens — it was composition. Rendered every pre-app screen with Playwright (real `next dev` for landing/login/signup; a real-component esbuild+Tailwind-CLI harness, same methodology as rounds 1–2, for the authenticated screens no live Supabase session in this sandbox can reach). At 1280px, landing/login/signup/every onboarding question step were a mobile-width column dead-centered in a huge empty desktop canvas — zero `lg:` treatment anywhere in those files. Home dashboards had a real but less severe version of the same issue (column-height mismatch in the `lg:grid-cols-[minmax(0,1fr)_320px]` layout — partially mitigated in production by `/app/layout.tsx`'s existing max-width cap, sidebar, and ambient-glow blobs, which the harness doesn't render). Brand gradient usage was thin (one headline word + one button on landing) and the icon-in-rounded-square+chevron card was the dominant, repeated composition primitive on both dashboards. Two `WebSearch` queries against current design-quality sources named these as specific, common "AI slop" tells (centered hero + one CTA, purple-indigo gradient confined to small spots, identical icon-card grids) — used to sharpen the diagnosis into a checkable list, not to copy any competitor's screen. Full detail, including the exact screenshots' findings and sources cited, in `UI_REDESIGN_REPORT.md`'s Round 3 section.

**Figma: genuinely available and used this time**, unlike rounds 1–2's honest "not exposed" finding. Built two real reference frames in a real Figma file (asymmetric two-column landing + onboarding-question composition, `BrandPanel`-style right-hand visual anchor instead of dead space) and used them as the direct layout reference for the code changes below. Canva: no genuine need arose (same as rounds 1–2 — every visual need is covered by CSS gradients/SVG/Lucide, no raster asset requirement).

**What shipped (composition-level, not just tokens — new authorization this round):** new shared `src/components/shared/BrandPanel.tsx` (desktop-only, 5 copy variants) plus `lg:` two-column restructuring on landing (`page.tsx`), login/signup, and `OnboardingShell.tsx` (fixes all 18 onboarding question steps via the one shared shell) — every one of these previously had zero desktop composition. Landing also got a real proof strip (checkable claims, not invented stats) and a sharper, Alxioum-specific subcopy rewrite. Home dashboards (`StudentHome.tsx`/`BusinessHome.tsx`) got column-height rebalancing only — 4 existing cards total moved to different `row-start` values across both files, zero new/removed cards, zero data changes — narrowing the column-height gap from ~380px to ~30px (Student) and ~380px to ~115px (Business) in the harness. No functional/data/route changes anywhere — confirmed via `git diff`, every change is JSX structure, className, or copy.

**Caught and fixed mid-round, not left in:** a mobile regression the landing proof-strip change introduced (`truncate` clipping the longer proof values illegibly at 390px) — caught by the mandatory mobile-regression screenshot pass, fixed, re-verified.

**Deliberately not done, with reasoning:** the two onboarding `ResultsScreen`s (already show real content, lower leverage than the empty-canvas question screens); the icon-in-rounded-square card primitive repeated across both Home dashboards (real finding, but fixing it app-wide is a materially bigger, riskier change than a composition pass and risks conflicting with Cato's standing "no Home redesign" direction if pushed further than the column-rebalancing done here — flagged as the best candidate for a dedicated future round); `choose-plan/page.tsx` (outside this round's named scope). Rounds 1–2's token/shadow/typography/focus-state/`.glass` work was kept and built on throughout, not replaced.

**Verification:** before/after Playwright screenshots, both themes, both viewports (390px/1280px), for every screen touched — landing, login, signup, StudentHome, BusinessHome, StudentOnboarding (representative of all 18 onboarding steps). Explicit mobile-regression pass on all 6 screens after every change. `col-start`/`row-start` re-grepped after the Home edits — still every grid item paired, none orphaned. `npx tsc --noEmit` and `npx next build` both clean (exit 0, all 42 routes) after harness deletion, run directly.

BLOCKERS: None.

NEXT TASK: None open from this round. Recommend a dedicated future pass on the icon-in-rounded-square card primitive repeated across both Home dashboards (flagged above) if further "still looks templated" feedback comes back after this round ships — that is the next-highest-leverage remaining composition gap, now that the pre-app screens' dead-canvas problem is fixed.

DEPLOYMENT STATUS (round 3): not deployed — committed locally on `claude/futureos-student-app-3ewdz6` via worktree `agent-a75f9963c88db1c5d`, not pushed. CEO/Cato handles merge + push + Render deploy trigger per standing process.

---

## DEV — DIRECT AI HOMEWORK HELP (per homework item), worktree `agent-a6c727fbe8e3c9a6e` — 2026-08-31

CEO asked for the AI to help students finish their actual homework, directly. Cato scoped it first and found the existing AI tutor (`/api/study/tutor` + `subjects/[subjectId]/homework-help/page.tsx`) is a dead end for this: it requires a real `study_subjects` row, but `homework` items only carry a free-text `subject` string with no link to `study_subjects`. Built a new, homework-native path instead of forcing a reuse.

**Shipped:**
- `src/app/api/school/homework-help/route.ts` — new route, closely mirrors `/api/study/tutor`'s structure and security pattern (`requireUser` → `checkEntitlement` (Plus-gated, 402 if not) → `ANTHROPIC_API_KEY` check (503 if missing) → body validation (400) → DB lookup). Resolves the homework row itself from `homework` scoped to `.eq("id", homeworkId).eq("user_id", user.id)` — never trusts a client-supplied subject/title — 404 if not found/not owned. System prompt adapts the tutor's teach-don't-just-answer philosophy, grounded in `homework.subject` + `.title` + `.priority` instead of a study_subject/topic. Reuses `callStudyAIForText` at the same `maxTokens: 900, effort: "low"` as the existing tutor. 502 on AI failure.
- `src/app/app/school/homework/[homeworkId]/help/page.tsx` — same UX pattern as the subject-based homework-help page (4 quick-action buttons: Hint/Explain/Example/Walkthrough, back link, chat-bubble rendering, "teaches the concept, won't just hand you the answer" footer). Loads the homework item via the existing `useHomework` hook, filtered client-side by `homeworkId` (no new dedicated single-row query needed — list is already small and cached per user). Shows subject/title/priority as the page header instead of requiring the student to retype the question; quick actions frame generically ("Give me a hint to get started on this homework") when the optional textarea is left blank.
- `src/app/app/school/homework/layout.tsx` — new `PaywallGate` wrapper scoped to this one subtree only, matching the exact pattern used by `subjects/`, `weak-topics/`, `quizzes/`, `progress/`, `flashcards/`. Does **not** touch `/app/school` itself (the homework list/CRUD stays free-tier).
- `src/app/app/school/StudentSchoolHome.tsx` — added a compact circular "AI help" icon button (Sparkles icon, `h-8 w-8`, same visual weight as the existing complete-toggle checkbox) next to `PriorityBadge` on each pending homework row, linking to `/app/school/homework/${hw.id}/help`. Nothing else in the card restructured.

**Scope decisions:**
- **No chat persistence.** Deliberately ephemeral — no new `homework_help_messages` table, no migration. Each request only carries the current message, not prior turns (the API body is `{ homeworkId, message }`, no history array). This matches the CEO's constraint of no new DB tables/columns and keeps the surface identical to the quick-action-button UX (each click is already a fresh, self-contained question). Trade-off: a student can't say "explain that differently" and have the AI recall its last reply within this route — flagging this now rather than silently deciding it doesn't matter, per instructions. If back-and-forth threading turns out to matter, the fix is a `study_tutor_messages`-shaped table plus a `homeworkId`/`homework_id` history read, exactly mirroring the existing tutor route.
- **Plus-gated**, consistent with every other AI-generation route in the app (free tier is CRUD-only). Wrapped via a route-scoped `PaywallGate` layout, not an ad hoc check in the page, so a non-Plus user sees the standard upgrade-activation screen instead of a raw 402.
- **Icon-only entry point, not a text pill.** Built a text+icon "AI help" pill first, but a mobile Playwright render (harness route, deleted before commit) showed it squeezed the flex-1 `subject: title` text into much harder truncation than before at narrow widths (320–390px) on cards with long combined subject+title strings — a regression risk in a codebase QA has flagged for exactly this overflow/truncation failure class before. Confirmed the truncation itself is pre-existing (same long-title stress case truncates hard even with zero new UI, just less so), then switched to a compact `h-8 w-8` icon-only circular button matching the checkbox's visual weight, which minimizes the width taken from the title.

**Verification:** `npx tsc --noEmit` and `npx next build` both clean (exit 0, all 43 routes, both run twice — once before, once after clearing a stale `.next` generated-types reference to the deleted harness route, same known artifact class as round 2's reconciliation). Rendered the new page + updated homework-card entry point through a real Playwright browser (`/opt/pw-browsers/chromium`) against a temporary unauthenticated harness route (exact JSX/classNames copy-pasted from the real merged files, static mock data, no live Supabase available in this sandbox) at 320px/390px/1280px — zero horizontal overflow (`scrollWidth === clientWidth`) at every width, harness route confirmed fully deleted via `git status` and a clean re-grep for its name across `src` and `.next`. `col-start`/`row-start` grepped clean across every touched file (none use CSS Grid — this feature never touches the Home dashboards' grid layout).

BLOCKERS: None.

NEXT TASK: None open from this task. If usage data later shows students wanting multi-turn threading on this screen (not just single-shot quick actions), the natural follow-up is a small migration adding a `homework_help_messages` table mirroring `study_tutor_messages`'s shape — flagged above, not built now per the no-new-migration constraint.

DEPLOYMENT STATUS: not deployed — committed locally on `claude/futureos-student-app-3ewdz6` via worktree `agent-a6c727fbe8e3c9a6e`, not pushed. CEO/Cato handles merge + push + Render deploy trigger per standing process.

---

## QA REVIEW — DIRECT AI HOMEWORK HELP (Dev's commit `683cbbe`) — 2026-08-31

Reviewed adversarially against the actual shipped code, not Dev's summary. This worktree started at `b73ec8a` (did not have Dev's commit); pulled Dev's 5 files in via `git checkout 683cbbe -- <paths>` (content-identical to `683cbbe`, confirmed via `git diff 683cbbe --stat` returning empty for all 4 app files) rather than a branch merge, so this worktree's own commit carries the reviewed code forward for Cato to reconcile.

**Read `git show 683cbbe` in full (5 files, +293/-1) — not sampled.**

**Security (the core thing that matters here) — holds up.** `src/app/api/school/homework-help/route.ts`'s DB lookup is `client.from("homework").select("subject, title, priority").eq("id", homeworkId).eq("user_id", user.id).maybeSingle()` — genuinely scoped, not just "id" alone. `client` comes from `requireUser(req)` → `supabaseForRequest(req)`, a user-scoped client that carries the caller's JWT (traced `src/lib/supabase/server.ts` in full) — not the service-role client (`src/lib/supabase/server.ts` documents that privileged client is reserved, unused by any route today, confirmed by reading it). So even if the `.eq("user_id", ...)` filter were ever dropped by mistake, RLS is a second, independent backstop. `homework`'s live policy is the standard `homework_all_own` (`for all to authenticated using (user_id = auth.uid()) with check (user_id = auth.uid())`) in `supabase/schema.sql:127`, and this diff touches zero schema/migration/grant files, so nothing here could have weakened it. Did **not** re-run a live impersonation test this round (no Supabase MCP tool was exposed in my available toolset this session, and this diff makes no schema/policy change) — carrying forward the 2026-08-27 live pass's direct impersonation-tested confirmation that `_all_own` policies including cross-account isolation hold in production, on unchanged schema. A user requesting help for a `homeworkId` they don't own gets a clean 404 (`maybeSingle()` returns null → `{error: "Homework not found."}`, 404), not another user's data.

**Paywall gating — genuine, two independent layers, both verified.** `src/app/app/school/homework/layout.tsx` wraps `children` in `<PaywallGate>`, byte-identical pattern to `subjects/layout.tsx`, `weak-topics/layout.tsx`, `quizzes/layout.tsx`, `progress/layout.tsx`, `flashcards/layout.tsx`, `coach/layout.tsx` (grepped all 8 `PaywallGate` usages in the repo). Read `PaywallGate.tsx` itself: `isEntitled(profile)` gates on `plan_status === "active"` or an unexpired trial, renders an activation screen instead of `children` otherwise — this is a real block, not cosmetic (no `children` render path exists in the non-entitled branch). Independently, the API route's own `checkEntitlement(client, user.id)` (server-side, re-reads `plan_status`/`trial_ends_at` fresh from `profiles`) 402s before any AI call or DB grounding lookup happens — traced this is not reachable-around by a non-Plus user hitting the API directly (e.g. via curl/devtools) even if they never load the page at all. Two independent enforcement points, matches every other AI route in the app.

**404 / not-found handling — traced, no crash, no infinite spinner.** `page.tsx`'s `if (homeworkLoading && !hw) return <Loading…>` then `if (!hw) return <"couldn't be found" + back link>` — a nonexistent or not-owned `homeworkId` resolves to the friendly not-found state once the (cheap, RLS-scoped) `useHomework` fetch completes, never a blank crash. Confirmed the parent `src/app/app/layout.tsx` gates all `/app/*` children (`loading || !user || !profile`) before this page's first render, so `user.id` is always resolved by the time `useHomework(user?.id)` runs — no false-negative "not found" flash during auth loading (same invariant QA already confirmed protects `/app/deadlines`).

**No chat persistence — confirmed true, not just claimed.** Grepped the new route file for `study_tutor_messages`/`.insert(`/`.upsert(` — zero writes anywhere in `route.ts` beyond the single scoped `.select()`. The route is a pure read-then-generate-then-respond function.

**Entry-point rendering — re-rendered myself, not taken on Dev's word.** Built a real-JSX Playwright harness (temporary unauthenticated route, exact `Card`/`CardContent`/`PriorityBadge`/`Sparkles`/`Link` components and classNames copy-pasted verbatim from the merged `StudentSchoolHome.tsx`, deleted before commit — confirmed absent via `git status` and a clean `next build` route list afterward) at 320/375/390px with two deliberately long subject+title combinations ("Advanced Placement Chemistry: Complete the stoichiometry and limiting-reagent worksheet problems 1 through 20", "English Literature and Composition: Write a five-paragraph comparative essay..."). Result: `document.documentElement.scrollWidth === clientWidth` at all 3 widths — **zero page-level horizontal overflow**. The title `<p>` itself reports `scrollWidth > clientWidth` at narrow widths, which is the *correct*, expected behavior of the pre-existing `truncate` class (ellipsis-clip, not a leak) — confirmed visually via screenshot that titles clip cleanly with an ellipsis and the icon button stays fully visible and aligned on every row. The new `<Link aria-label="Get AI help with this homework" title="Get AI help">` renders a consistent 32×32px (`h-8 w-8`) hit target at all 3 widths (measured via `boundingBox()`, not eyeballed) — has both an `aria-label` and a native `title` tooltip, so it's not an unlabeled icon-only control. **Minor, non-blocking note**: 32px is below the commonly-cited 40-44px "comfortable" touch-target guidance (though it clears WCAG 2.5.8 AA's 24×24px minimum), but it's not a regression introduced by this diff — an identical `h-8 w-8` interactive button already exists in this codebase (`src/app/app/school/subjects/[subjectId]/session/page.tsx:393`, the AI-tutor chat's own back/close control) using the exact same size for the exact same "compact icon button next to chat/tutor UI" role. Flagging for a possible future accessibility pass across both sites together, not sending this back to Dev alone — inconsistent to hold one new icon button to a stricter bar than the existing one it was visually modeled on.

**Build gates — run directly, not taken on Dev's report.** This worktree's `HEAD` (`b73ec8a`) did not originally contain Dev's files — my first `tsc`/`build` run silently passed against the *old* code with the new route completely absent from the route list (a process trap worth naming: `git show`/`git diff` can read a commit's blobs without the files being on disk, so a naive "read the diff, then run the existing build" sequence can silently validate the wrong tree). Caught this from the build's route list missing `/api/school/homework-help` and `/app/school/homework/[homeworkId]/help` entirely, corrected by pulling Dev's actual files onto disk, then re-ran both clean: `npx tsc --noEmit` — clean (no output/errors). `npx next build` — clean, **43 routes** including `ƒ /api/school/homework-help` (5.69 kB) and `ƒ /app/school/homework/[homeworkId]/help` (182 kB First Load JS), both dynamic as expected for an auth+DB-backed route/page.

**Consistency spot-checks:** `callStudyAIForText({ system, messages: [{role:"user", content: message}], maxTokens: 900, effort: "low" })` matches the existing `/api/study/tutor` route's call shape exactly (same file, same helper, same maxTokens/effort). `homework.priority` is `not null` with a `check` constraint (`schema.sql:121`) so the system-prompt interpolation can never see an unexpected value. `StudentSchoolHome.tsx`'s diff is a single, isolated hunk (verified via `git show`) — does not touch the nearby `toggleHomework` XP-guard logic fixed in an earlier round, so no regression risk there.

**Verdict: sign-off.** This is a real, adversarial pass — full diff read, DB-scoping traced through `requireUser`→RLS, both paywall layers independently verified (not just read), 404 path traced, persistence claim grepped and confirmed false-negative-free, own Playwright render at 3 real widths with adversarial long-title data (not just a JSX read), own `tsc`/`next build` run against the actual merged code (caught and corrected my own tooling mistake of initially validating the wrong tree). One non-blocking note handed to Cato/Product for a possible future pass: the new 32px icon button and the pre-existing `session/page.tsx:393` icon button it was modeled on are both under the ~40-44px "comfortable" touch-target norm — not a regression, not blocking this feature, worth a consistency pass if/when touch-target sizing gets revisited generally.

BLOCKERS: None.

NEXT TASK: None open from this review. Handing to Cato for final sign-off/merge per standing process.

DEPLOYMENT STATUS (this QA pass): not deployed — reviewed and committed locally on `claude/futureos-student-app-3ewdz6` via worktree `agent-a4197bcda20129b17`, not pushed. CEO/Cato handles merge + push + Render deploy trigger per standing process.

---

## DEV — SIGNUP CONFIRMATION: CODE ENTRY INSTEAD OF EMAIL LINK, worktree `agent-a0eb5d9b08b1f7b53` — 2026-08-31

CEO wanted signup confirmation to work via a typed 6-digit code instead of "click the link" — pure app-side change, `supabase.auth.verifyOtp`/`.resend()` are existing Supabase Auth APIs, no schema/migration touched. Email template + Resend SMTP config handled separately in the Supabase dashboard, not this task's concern.

**Shipped, `src/app/signup/page.tsx` only:**
- Extracted the profile-creation + `/choose-plan` redirect (previously only in the immediate-session branch) into a shared `createProfileAndContinue(userId, fullName)` — still a plain `.insert()`, not `.upsert()` (same PostgREST column-grant restriction as documented for every other `profiles` write in this codebase), called from both the immediate-session path and the new post-verify path.
- Replaced the static "check your email, click the link" screen with a code-entry form: single numeric input (`inputMode="numeric"`, `maxLength={6}`, `autoComplete="one-time-code"`, digits-only filter on change — deliberately not 6 separate boxes, no UX case for the extra complexity here), submits to `supabase.auth.verifyOtp({ email, token, type: "signup" })`. Success (`data.session` present) runs the shared profile-creation function; failure surfaces Supabase's real error message directly (covers both wrong code and expired code — Supabase returns "Token has expired or is invalid" for both, verified via a forced-error harness render, not assumed).
- "Resend code" calls `supabase.auth.resend({ type: "signup", email })` with a 45s client-side cooldown (starts immediately after the initial `signUp()` call too, since that already sent the first email) — disabled button, live countdown via `setInterval`, to protect Resend's send quota per the task's ask.
- Kept the exact pre-existing visual pattern (centered icon+heading+subtext inside the same `<main>`, no `BrandPanel` desktop split) — this was already how the static screen looked, and a short-lived confirmation step isn't the right candidate for the two-column desktop treatment Product added to the primary form views; left as-is rather than inventing new layout, per the task's own default-to-leaving-it framing.
- `src/app/login/page.tsx`: read in full, made no changes. A user who clicks the link instead of entering the code still lands on `/login` via the unchanged `emailRedirectTo`, and `supabase-js`'s `detectSessionInUrl: true` (already configured in `src/lib/supabase/client.ts`) picks up the session from the URL fragment automatically — login's existing first-login profile-creation branch (`!profile` → insert → `/choose-plan`) still applies whether the session came from a password login or a link click, this was already true before my change and I didn't touch it.

**Edge cases handled explicitly:** expired code and wrong code (both surfaced via Supabase's real error message, not swallowed); a user already holding a session landing on this screen (structurally unreachable — `needsConfirmation` only flips true from `handleSubmit`'s no-session branch, and a signed-in user resubmitting the signup form gets Supabase's "already registered" error before that point, never reaching the code screen).

**Verification:** `npx tsc --noEmit` and `npx next build` both clean (exit 0, 43 routes, `/signup` at 6.15 kB) — run once with the harness route present (caught the same stale-`.next`-generated-types artifact class documented earlier in this file from a deleted temp route, cleared `.next`, re-ran clean) and once after the harness was fully removed and confirmed absent via `git status`. Rendered the code-entry screen (default, wrong/expired-code-error, and just-resent-with-cooldown states) through a temporary unauthenticated Playwright harness (`/opt/pw-browsers/chromium-1194`, JSX copy-pasted verbatim from the real merged file) at 390px in both themes (theme forced via `localStorage` before nav, since the app's own init script defaults to light when no stored preference exists) — `scrollWidth === clientWidth` in all 6 combinations, zero horizontal overflow, screenshots confirm clean rendering matching the established icon+heading+subtext pattern in both light and dark.

BLOCKERS: None.

NEXT TASK: None open from this task. Handing to QA for review.

DEPLOYMENT STATUS: not deployed — committed locally on `claude/futureos-student-app-3ewdz6` via worktree `agent-a0eb5d9b08b1f7b53`, not pushed. CEO/Cato handles merge + push + Render deploy trigger per standing process.

---

## QA REVIEW — SIGNUP OTP CONFIRMATION (Dev commit `5efd0e73`) — 2026-08-31

Reviewed `git show 5efd0e73d0b3b03065d1efd357db4e540f2328bc` in full (only `PROJECT_STATE.md` + `src/app/signup/page.tsx` touched, +170/-18 in the code file — not sampled). This worktree branched from `e55fe07` before Dev's commit was pushed anywhere, so the merge/cherry-pick tools were unavailable (blocked by the environment's own worktree-isolation guard); brought the file onto disk by writing byte-identical content copied from `git show <commit>:src/app/signup/page.tsx` and confirmed `git diff <commit> -- src/app/signup/page.tsx` was empty before testing, exactly to avoid the "validated the wrong tree" trap this same file's history already flagged once.

**Real bug found — `createProfileAndContinue`'s failure path is unreachable/invisible on the new OTP-verify call site.** The shared helper sets `error` state on a failed `profiles` insert (`setError("Your account was created, but we couldn't set it up yet. Try logging in.")`), which is rendered by the *original* signup-form JSX (`{error && <p className="text-sm text-danger">{error}</p>}` inside the `handleSubmit`-path return). The new `needsConfirmation` (OTP-entry) branch's JSX only renders `{verifyError}` and `{resendMessage}` — it never renders `{error}` anywhere. So: user enters a correct code → `verifyOtp` succeeds (session + user now exist, real Supabase auth session live in the browser) → the `profiles` insert fails for any reason (transient network blip, RLS/grant edge case, etc.) → `setError(...)` fires into a state variable nothing in the currently-rendered tree reads → `verifyLoading` still flips back to `false` so the button re-enables → the user sees **no error message at all**, just the button stop spinning with no explanation, no redirect, stuck on the code-entry screen holding a real but profile-less session.
  - Compounding severity, traced through the rest of the app: if that stuck user then navigates to `/app` (or refreshes and the app's session-restore routes them there), `app/layout.tsx`'s gate is `if (loading || !user || !profile) return <LoadingScreen message="Signing you in…" />` — there is no fallback/redirect when `user` exists but `profile` is permanently `null`; the user hits an infinite "Signing you in…" spinner with zero recovery path from the UI itself. The only actual recovery path is `/login`, whose `handleSubmit` re-queries `profiles`, finds none, and inserts it (confirmed by reading `src/app/login/page.tsx` — untouched by this diff) — but the OTP screen never tells the user to do that in this failure case, and there's no visible signal that anything went wrong to prompt them to try it.
  - This is exactly the "call-site drift" class the task asked me to check for: the two `createProfileAndContinue` call sites are identical in *what they call*, but not identical in *what the surrounding UI does with the result* — the immediate-session path (original form) can show its own failure, the post-verify path (new) silently can't.
  - **Fix needed (for Dev):** either render `{error}` in the `needsConfirmation` branch's JSX too (simplest — same state, just also read it there), or have `createProfileAndContinue` report failure through a shared/passed-in setter so both branches display it. Also worth having `app/layout.tsx` handle a genuinely-`null` profile with a real user by redirecting to `/login` (or `/choose-plan` with a defensive insert) instead of spinning forever — a defensive backstop, not just a symptom fix, since the layout gate itself has no escape hatch for this state regardless of how it's reached.

**Rate limiting / abuse (the substantive question) — the 45s client-side cooldown is real UX, not a real security control, but that's fine here because Supabase's own server-side limits are the actual backstop.** Read the code: `resendCooldown` is plain `useState`, reset to `0` on every fresh mount/page load, and gates nothing except the button's `disabled` prop — `handleResend` itself also checks `resendCooldown > 0` before calling `.resend()`, but that check runs in the browser's own JS, so it protects nothing against a caller that skips the UI (refresh the tab to reset React state, or call `supabase.auth.resend({ type: "signup", email })` directly against the public Supabase REST/GoTrue endpoint with the anon key, which is exposed client-side by design). Confirmed via `WebSearch` against Supabase's own docs (not assumed): Supabase Auth enforces its own **server-side** rate limits independent of any app code — a per-IP token-bucket limiter across auth endpoints (burst capacity ~30), a per-identity OTP-request cooldown of 60s (a fresh `resend`/`signInWithOtp` for the same email is rejected server-side inside that window regardless of what the client sends), and a separate email-sending-quota limit (2/hr on Supabase's default SMTP, 30/hr once a custom SMTP/Resend provider is configured — which PROJECT_STATE.md's own account of this task says is being set up separately). **Verdict: the 45s client cooldown is cosmetic/UX-only and is trivially bypassable exactly as suspected (page refresh, or a direct API call) — but this is not a live vulnerability, because Supabase's real, server-side, per-email/per-IP rate limiting is what actually protects the Resend sending quota, and that layer is independent of anything this diff does or doesn't do.** Not blocking. Worth noting to Cato as a documentation/expectation-setting item only: don't let "we added a 45s cooldown" get repeated later as if it were the actual rate-limit control — it isn't, Supabase's server-side config is, and that's a dashboard setting outside this diff's scope (consistent with the task's own framing that SMTP/email config is being handled separately).

**Code/token handling — clean.** Confirmed via a live interactive Playwright test (not just reading the regex): typed `"12ab34#$56789"` into the real running code input and the DOM value after typing was `"123456"` — the `.replace(/[^0-9]/g, "").slice(0, 6)` filter genuinely strips non-digits and caps length, matching Dev's claim. Grepped the whole file for `console.log`/`localStorage`/any second consumer of the `code` state — the only place `code` (as `trimmed`) is used is the single `verifyOtp({ email, token: trimmed, type: "signup" })` call; it is never logged, never persisted (no `localStorage`/`sessionStorage`/cookie write anywhere in the file), and never sent anywhere else.

**Error message honesty — confirmed, not taken on Dev's word.** Read `handleVerifyCode`: on `verifyErr` it does `setVerifyError(verifyErr.message)` directly — Supabase's real error text is surfaced verbatim for both wrong-code and expired-code cases (both produce Supabase's own message, e.g. "Token has expired or is invalid" per Supabase's documented behavior for `verifyOtp`), nothing swallowed or genericized. Same pattern in `handleResend` (`setVerifyError(resendErr.message)`). The only *invented* copy in the whole flow is the generic fallback `"Couldn't verify that code. Try again or request a new one."`, used only in the narrow case where `verifyOtp` returns no error but also no `session`/`user` — a reasonable, honestly-labeled fallback for a genuinely edge case, not a cover for a real error being hidden.

**`createProfileAndContinue` — confirmed plain `.insert()`, never `.upsert()`,** both call sites use the identical function/call shape (`createProfileAndContinue(userId, fullName)`) with no drift in *what* is written — the only difference is the `fullName` argument's fallback chain in the post-verify path (`name.trim() || (data.user.email?.split("@")[0] ?? "Student")` vs. the immediate-session path's plain `name.trim()`), which is a strict superset of safety (defends against `name` being empty if the component ever remounted between initial submit and verify) and not a regression. The *drift that does matter* is the error-rendering gap documented above — the call itself is identical, the surrounding UI isn't.

**`/login` "pre-existing" claim — verified accurate, not taken on Dev's word.** `git show 5efd0e73... --stat` confirms only `PROJECT_STATE.md` and `src/app/signup/page.tsx` were touched by this commit; `src/app/login/page.tsx` is untouched. Read `login/page.tsx` in full: `handleSubmit` is genuinely the only place profile-creation runs (queries `profiles`, inserts if absent, then routes) — a user who lands there via the `detectSessionInUrl` link-click fallback (already-authenticated, `/login`'s form never auto-submits) does have to actually submit the login form before a profile row is created for them. Confirmed pre-existing and correctly out of scope for this diff, not touched or worsened by it.

**Render check — own render, not Dev's screenshots (Dev's harness was gone before this commit, as expected).** Started the dev server directly (`npx next dev`), temporarily forced `needsConfirmation`'s initial `useState` to `true` (plus a placeholder `email` value) purely to reach the branch without a live Supabase session, screenshotted via Playwright/Chromium at 390px in both themes (theme forced via `localStorage.setItem('alxioum_theme', ...)` before navigation, matching the app's own init-script convention), then reverted both temporary edits and re-confirmed `git diff 5efd0e73... -- src/app/signup/page.tsx` was empty again before running the build gates. Both renders are clean: code input, "Confirm account" button, "Resend code" link, and the "prefer the link" fallback copy all render correctly with no overflow/wrap issues in dark and light, `:focus-visible` ring shows correctly on the autofocused input. No hydration/console errors in the dev-server log for either load (only pre-existing infra noise: a benign multi-lockfile workspace-root warning, unrelated to this diff).

**Build gates — run directly.** `npx tsc --noEmit`: clean (exit 0). `npx next build`: clean (exit 0), all 43 routes including `/signup` at 6.15 kB.

**Verdict: NOT signed off as-is — one real bug (silent-failure UI gap on `createProfileAndContinue`'s error path in the new OTP-verify branch) needs a fix before this ships.** Everything else held up under adversarial testing: digits-only filtering (verified interactively), error-message honesty (verified by reading the actual code path, not Dev's description), no `.upsert()`/no call-site drift in the write itself, the `/login` pre-existing-behavior claim (verified via `git show --stat` + full file read), and the resend-cooldown rate-limiting question (verified against Supabase's own documented server-side limits, not assumed) — the 45s client cooldown is real but purely cosmetic, and that's fine because Supabase's server-side rate limiting is the actual, independent backstop. Sending back to Dev for: (1) render `{error}` in the `needsConfirmation` branch (or route both call sites through one shared error-display setter), (2) consider a defensive fallback in `app/layout.tsx` for the `user`-without-`profile` state instead of an unrecoverable infinite spinner. Will retest both once fixed.

BLOCKERS: Bug above blocks sign-off — not deployable as-is (silent failure with no recovery path in a real, if narrow, failure case).

NEXT TASK: Dev fixes the two items above; QA retests the `needsConfirmation` branch's error rendering (forced-failure case) and the `app/layout.tsx` defensive fallback, then hands to Cato.

---

## DEV — FIX: SIGNUP OTP SILENT PROFILE-CREATION FAILURE, worktree `agent-a542a83380afadd79` — 2026-08-31

Fixed the bug QA sent back (`createProfileAndContinue` failure after a successful `verifyOtp` was invisible — session live, no profile row, no error shown, no recovery). This worktree branched from `e55fe07`, before Dev's OTP commit (`5efd0e73`) had been pushed anywhere, so cherry-picked QA's own commit `a9fb383` (which already carries `5efd0e73`'s code byte-identical, per QA's own note, plus their review) directly onto this branch — clean cherry-pick, confirmed via `git diff` before editing.

**`src/app/signup/page.tsx`:**
- Added `verifiedUserId` state, set the moment `verifyOtp` succeeds (real session/user exist from that point on). Distinguishes "still entering the code" from "code was correct, the follow-up `profiles` insert failed" — the latter is not a code-verification failure and re-submitting the same (now-consumed) code would just produce a confusing "already used" error, so it gets its own branch instead of falling through to the code-entry form.
- New recovery view, rendered when `verifiedUserId && error`: explains the account is confirmed but setup didn't finish, shows the real `error` message (previously swallowed), and offers two ways forward — "Try again" (calls new `handleRetryProfile()`, which re-runs just the `profiles` insert against the existing session, no code re-entry needed) and "Log in instead" (links to `/login`, whose `handleSubmit` already re-queries/inserts a missing profile — confirmed working, not new).
- Normal code-entry view: `{(verifyError || error) && ...}` now reads `error` too (previously only `verifyError`/`resendMessage`), so the shared error state is never silently dropped regardless of which path set it. In practice `error` is only ever truthy alongside `verifiedUserId`, so this mainly serves as the documented shared-display QA suggested — the dedicated recovery view is what actually fires for this bug.
- `handleVerifyCode` now clears `error` (not just `verifyError`) at the start of each attempt, for the same hygiene reason `handleSubmit` already clears `error` at the start of its own attempts.

**`src/app/app/layout.tsx`:** added a fallback for `user` truthy + `profile` genuinely `null` (not just still loading). `AuthProvider`'s initial `loading` flip to `false` only happens after its `loadProfile` call is awaited (read `AuthProvider.tsx` in full to confirm this, not assumed), so by the time `loading` is `false` in this gate, a missing `profile` is real, not a race — there's no code path that would make one appear later on its own. Split the old single `if (loading || !user || !profile)` gate into `if (loading || !user)` (spinner, unchanged) and a new `if (!profile)` branch showing a real message ("We couldn't find your profile… Logging back in usually fixes this.") with a "Go to log in" button — same visual language as the new signup recovery view (danger-tinted icon badge, heading, subtext, single CTA).

**Reproduced the bug live before fixing it, not assumed from QA's report:** since this sandbox has no real Supabase network access (confirmed again this session — `curl` to the project's `*.supabase.co` host returns a 403 tunnel failure, same org egress block prior QA passes hit), set a temporary local-only `.env.local` (gitignored, deleted before commit) pointing `NEXT_PUBLIC_SUPABASE_URL` at a fake host, ran the real dev server, and used Playwright's network-route interception to mock exactly the 3 calls this flow makes: `POST /auth/v1/signup` (no session → code-entry screen), `POST /auth/v1/verify` (success, real session), `POST /rest/v1/profiles` (mocked `409` to simulate a real insert failure). On the pre-fix code (`git stash` to the QA-reviewed commit), typing the correct code and submitting reproduced exactly what QA described: the button re-enabled, code input still showed the entered digits, and the rendered DOM had zero error text anywhere — confirmed via `page.locator("main").innerText()` and a screenshot. Restored the fix (`git stash pop`) and re-ran the identical script: the recovery view now renders with the real error message and both buttons. Then verified the retry path end-to-end with a second Playwright run (profile insert mocked to fail once, then succeed) — "Try again" correctly calls just the profile insert (no re-verify), and on success navigates to `/choose-plan`.

**Regression-checked the rest of the OTP flow with the same interception technique**, all passing: correct code still verifies and navigates to `/choose-plan` on the first try when the profile insert succeeds; wrong/expired code still shows Supabase's real error inline with no navigation; resend cooldown still starts at 45s and the button is still disabled during it.

**`app/layout.tsx` fallback verified separately:** seeded a fake, valid Supabase session directly into `localStorage` (matching the real `sb-<project-ref>-auth-token` key `supabase-js` uses) so `getSession()` resolves with a user with no network call, mocked the `profiles` GET to return `[]` (genuinely-absent row, not a timing artifact), and loaded `/app` directly. The new fallback renders correctly in both themes at 390px — confirmed via screenshot, no layout/overflow issues, matches the signup recovery view's visual language.

**Verification:** `npx tsc --noEmit` clean (exit 0). `npx next build` clean (exit 0, all 43 routes). `.env.local` and all scratch Playwright scripts/screenshots were outside the repo or deleted before commit — `git status` confirms only the two intended source files changed.

BLOCKERS: None.

NEXT TASK: Handing back to QA to re-verify the fix (forced-failure recovery view, retry path, `app/layout.tsx` fallback) before Cato/CEO merge and deploy.

DEPLOYMENT STATUS: not deployed — committed locally on `claude/futureos-student-app-3ewdz6` via worktree `agent-a542a83380afadd79`, not pushed, per instruction (QA re-verifies before merge/deploy).

DEPLOYMENT STATUS (this QA pass): not deployed — reviewed and committed locally on `claude/futureos-student-app-3ewdz6` via this worktree, not pushed. Do not merge/deploy until the bug above is fixed and retested.

---

## QA RE-REVIEW — SIGNUP OTP FIX (Dev commit `91e94bb`) — 2026-08-31

Second pass on this feature. Adversarial, not a rubber stamp — independently reproduced everything rather than trusting Dev's report, and found one new real regression the fix itself introduces.

**Lineage verified as real, not just claimed.** This worktree started at `e55fe07` (no OTP code at all). Cherry-picked `3b94a79` (my own prior review commit, which QA's own note confirms is content-identical to `a9fb383` — verified again here via `git diff a9fb383 3b94a79` returning empty) — clean, no conflicts. Then cherry-picked `91e94bb` (Dev's fix) directly on top — also clean, no conflicts. `git diff 91e94bb HEAD -- src/app/signup/page.tsx src/app/app/layout.tsx` is empty: this worktree's HEAD (`f0d0011`) carries byte-identical content to `91e94bb`. The claimed history (fix built on top of my sign-off commit, which carries the OTP feature) is genuine, confirmed by content diff, not commit-message trust.

**Read `git show 91e94bb` in full (3 files, +113/-4) — not sampled.** Matches Dev's description: `verifiedUserId` state set on successful `verifyOtp`; new recovery view gated on `verifiedUserId && error`; `error` now also read in the normal code-entry view's error display; `handleVerifyCode` clears `error` at the start of each attempt; `app/layout.tsx` split into `if (loading || !user)` (unchanged spinner) + new `if (!profile)` fallback (real message + "Go to log in").

**Independent repro, built from scratch (not Dev's harness).** No live Supabase access in this sandbox either (same org egress block, unrelated to this diff). Applied the actual committed `91e94bb` file content into a real `npx next dev` server via a temporary `.env.local` pointing at a fake Supabase host, and used Playwright network-route interception (own script, not Dev's) to mock `auth/v1/signup`, `auth/v1/verify`, and `rest/v1/profiles`:
- Forced a `409` on the first `profiles` POST after a successful `verifyOtp` (real session established, matching mocked JWT/session shape) — the recovery view rendered with the exact real error text (`"Your account was created, but we couldn't set it up yet. Try logging in."`), both buttons present. Confirmed this is the genuine `createProfileAndContinue` failure path, not a stub.
- Clicked "Try again" — confirmed via network log it fires only a second `POST /rest/v1/profiles` (no second `POST /auth/v1/verify` — the already-consumed code is never resubmitted), and on that second call succeeding, navigated cleanly to `/choose-plan`. `handleRetryProfile` does exactly what it claims: retries only the profile insert against the live session.
- Regression suite (own script, 3 independent scenarios): correct code + successful insert on the first try → `/choose-plan` (PASS). Wrong/expired code → Supabase's real error text rendered inline, no navigation (PASS). Resend cooldown → button disabled, reads `"Resend code (45s)"` immediately after the first send (PASS). All three hold, unchanged from before this fix.
- Render check, both new UI states, 390px, both themes (own Playwright harness, `localStorage`-forced theme matching the app's own init-script convention): signup recovery view and `app/layout.tsx`'s `!profile` fallback both render cleanly — `document.documentElement.scrollWidth === clientWidth` true in all 4 combinations (recovery×light, recovery×dark, fallback×light, fallback×dark), no truncation/overflow, danger-tinted icon badge + heading + subtext + CTA(s) all visible and correctly styled. Screenshots taken and reviewed directly, not assumed from a JSX read.
- Noted, not a bug: a pre-existing client-hydration warning (`<html>` `className` gains `light` after mount) shows on every page load in this dev-mode harness — traced to `src/components/providers/ThemeProvider.tsx`'s deliberate pre-hydration inline theme script (`themeInitScript`, documented in its own code comment as intentional flash-prevention), confirmed unrelated to this diff and pre-existing app-wide.

**Item 4 (the "does `loading=false` guarantee `loadProfile` resolved" claim) — Dev's claim is FALSE for the case that actually matters, and this is a real, reproducible regression.** Read `AuthProvider.tsx` in full. Dev's claim is correct only for the *initial* mount path: the first-load effect does `getSession().then(...).finally(() => setLoading(false))`, so `loading` genuinely gates the first profile fetch. But `AuthProvider` also has a second, independent path — `supabase.auth.onAuthStateChange((_event, session) => { setUser(...); if (session?.user) loadProfile(session.user.id); ... })` — and this handler **never touches `loading` at all**. Every subsequent sign-in event (a normal login, an OTP verify, an OAuth callback, anything that fires `onAuthStateChange` after the app has already resolved its initial "no session" state with `loading=false`) sets `user` truthy immediately and kicks off `loadProfile` *unawaited*, while `loading` stays permanently `false` from the initial mount. This is a real, pre-existing gap in `AuthProvider`'s design — invisible before this diff because the old `app/layout.tsx` gate (`loading || !user || !profile`) just kept showing the soft, self-correcting `"Signing you in…"` spinner during that window. Dev's new `!profile` branch replaces that spinner with a hard, dead-end-looking `"We couldn't find your profile… Go to log in"` screen — and that screen can now flash for a **completely normal, fully-onboarded returning user on an ordinary login**, not just the narrow silent-failure case this fix was built for.
  - **Reproduced end-to-end, own script**, mocking `/login`'s real flow: `signInWithPassword` succeeds → `onAuthStateChange` fires (`AuthProvider`'s own `loadProfile`, a `select=*` GET, kicked off unawaited) → `login/page.tsx`'s own *separate*, independent `.select("onboarding_completed, billing_interval")` query resolves (a different GET, same table) → `router.push("/app")` fires *before* `AuthProvider`'s own GET has resolved. Added realistic latency (500ms) to `AuthProvider`'s GET only, matching its actual request start time (logged, ~1ms apart from the login page's own query) — confirmed via timestamped logs that both requests start almost simultaneously, so any real-world scenario where `AuthProvider`'s request happens to take even a few hundred ms longer than the login page's own request (plausible: HTTP/2 connection contention, mobile network jitter, server-side queuing — not a contrived edge case) reproduces this. At 50ms after navigating to `/app`, the rendered page is the new `"We couldn't find your profile"` fallback screen — for a user whose account and profile are completely fine and fully onboarded. ~600ms later, once `AuthProvider`'s own fetch resolves, the page silently flips to the real dashboard. Screenshotted (both the false-positive state and the corrected state) — this is not a theoretical race, it's directly observable and reproduced multiple times at realistic delay values (500ms; also reproduced once at 400ms, did not reproduce at 120–250ms in this specific harness's timing, consistent with it being a genuine, delay-magnitude-dependent race rather than either "always broken" or "impossible").
  - **Severity:** lower than the original bug (that one had zero recovery path, ever; this one self-corrects within roughly a second once the lagging fetch resolves) — but it is a real, user-visible regression on the single most common code path in the app (every normal login), and if a user acts on it before it self-corrects (clicks "Go to log in" during the flash window), it actively logs out/redirects a fully-working account for no reason, which is worse than the soft spinner it replaced.
  - **Fix needed (for Dev):** the `!profile` fallback needs to distinguish "genuinely resolved, no profile row" from "a profile fetch is in flight for a just-authenticated user." The cleanest fix is in `AuthProvider.tsx` itself, not `app/layout.tsx`: make the `onAuthStateChange` handler track in-flight profile loads the same way the initial-mount effect already does (e.g., a `profileLoading` flag set before calling `loadProfile` and cleared after, exposed via the context, with `app/layout.tsx` gating on `loading || profileLoading` before evaluating `!profile`) — but this needs care to *not* re-trigger a full loading gate on routine `TOKEN_REFRESHED` events for an already-rendering, already-profiled user (which would introduce a new flicker regression of its own). Flagging the design constraint, not prescribing the exact implementation — that's Dev's call.

**Verdict: NOT signed off.** The original silent-failure bug (this task's starting point) is genuinely fixed — recovery view, retry path, and regression suite all verified independently and hold up. But the fix introduces a new, real, independently-reproduced regression: a false-positive "profile not found, log back in" screen that can flash for ordinary, fully-working returning users on a normal login, caused by a pre-existing gap in `AuthProvider`'s `onAuthStateChange` handler that Dev's own stated reasoning ("`loading` being false guarantees `loadProfile` resolved") does not actually hold for. Sending back to Dev for the `AuthProvider` race fix described above; will re-verify with the same login-race repro script once fixed, plus the previously-passing recovery-view/retry/regression suite to confirm no new regression from whatever fix lands.

BLOCKERS: Bug above blocks sign-off — a real, reproduced regression on the ordinary login path, not deployable as-is.

NEXT TASK: Dev fixes the `AuthProvider` onAuthStateChange/loading-state gap described above (design constraint: must not introduce a new loading-flicker regression on routine token-refresh events for already-authenticated users). QA re-verifies via the same login-race Playwright repro (mocked `signInWithPassword` + delayed `AuthProvider` profile fetch) plus re-confirms the recovery view/retry path/regression suite still hold, then hands to Cato.

---

## DEV — FIX: AUTHPROVIDER onAuthStateChange PROFILE-LOAD RACE (QA regression on `00bcb5d`) — 2026-08-31

Fixed the regression QA sent back: `AuthProvider`'s `onAuthStateChange` handler kicks off an unawaited `loadProfile` on every auth event (not just the initial mount) without ever touching `loading`, so a fresh sign-in racing `login/page.tsx`'s own separate profile query could land on `app/layout.tsx`'s new `!profile` fallback for a completely healthy, fully-onboarded account for a few hundred ms.

**Worktree note:** this worktree's branch (`worktree-agent-adf465bc9bd0347aa`) had not yet received the OTP-signup feature or either round of the fix/QA-regression commits (`5efd0e73` → `3b94a79`/`3b1f11b` → `91e94bb`/`f0d0011` → `00bcb5d`) — confirmed via `git log`/`git branch --all --contains`. That whole chain lives on `worktree-agent-aa76c0857f143361b` and is a straight linear descendant of this worktree's own prior HEAD (`e55fe07`), so fast-forward-merged `00bcb5d` in (`git merge --ff-only`, no conflicts, no rebase) to get onto the exact code QA reviewed before making the fix on top.

**`src/components/providers/AuthProvider.tsx`:**
- Added `profileLoading` state, exposed on the context alongside `loading`/`user`/`profile`. True only while a profile fetch for a *newly-seen* user id is in flight (a fresh sign-in, or a user switch) — deliberately not set for repeat events on an already-known user id.
- Added a `lastUserIdRef` (a `useRef<string | null>`) tracking the user id the provider has already started/finished loading a profile for. The initial-mount `getSession()` effect now sets this ref alongside its existing `loading`-gated load. `onAuthStateChange`'s handler now branches on `nextUserId !== lastUserIdRef.current`:
  - **Different/new user id** (a real sign-in event, or a user switch): this is the case the previous fix's `loading` check missed entirely. Sets `profileLoading(true)`, updates the ref, calls `loadProfile(nextUserId).finally(() => setProfileLoading(false))`.
  - **Same user id as already loaded** (routine `TOKEN_REFRESHED`, `USER_UPDATED`, a duplicate `INITIAL_SESSION` fire racing the mount effect, etc.): still calls `loadProfile` in the background to stay fresh (preserves the pre-existing "refresh on any auth event" behavior), but never touches `profileLoading` — so no already-rendering, already-authenticated screen can flicker from this branch, regardless of how slow that background fetch is.
  - No session (sign-out): resets the ref to `null` and `profileLoading` to `false` alongside the existing `setProfile(null)`.

**`src/app/app/layout.tsx`:** gate changed from `if (loading || !user)` to `if (loading || !user || profileLoading)` before falling through to the `!profile` genuine-failure fallback. Updated the stale comment (previously claimed `loading` alone was sufficient — QA's finding disproved that for the `onAuthStateChange` path) to explain both `loading` (initial mount) and `profileLoading` (every subsequent auth event) are what jointly guarantee a `null` profile reaching that branch is real, not in-flight.

**Reproduced QA's exact race first, confirmed it's gone, confirmed no new flicker — all via Playwright network-interception against a real `npx next dev` server** (no live Supabase access in this sandbox, confirmed blocked again this session; temporary gitignored `.env.local` pointing at a fake host, deleted before commit):
1. **Repro against pre-fix code first (`git stash` back to `00bcb5d`'s `AuthProvider.tsx`/`layout.tsx`):** re-ran QA's own repro script (mocked `signInWithPassword` fast, `AuthProvider`'s `select=*` profiles GET delayed 500ms, `login/page.tsx`'s own separate query fast) — confirmed the bug reproduces: at 50ms after navigating to `/app`, the rendered body is exactly `"We couldn't find your profile… Go to log in"` for a fully healthy, onboarded account; ~600ms later it self-corrects to the real dashboard. This confirms the repro script is valid before trusting any "fixed" result from it.
2. **Restored the fix (`git stash pop`), re-ran the identical script:** at 50ms after navigating to `/app`, the rendered body is now the `"Signing you in…"` loading screen (not the dead-end fallback), then settles into the real dashboard once the fetch resolves. Fallback text never appears at any sampled point.
3. **Stress test at a wider delay:** same repro with the `AuthProvider` profile GET artificially delayed 2000ms (far exceeding the original 500ms) and the render polled every 100ms for 2.5s post-navigation — fallback text never appears at any sample; final state is the real dashboard.
4. **Token-refresh flicker test (the explicit QA constraint), own script:** logged in normally, let `/app` fully settle on real content, then fired a **real** `TOKEN_REFRESHED` event through the page's own live Supabase client (`supabase.auth.refreshSession()` — the same public API the SDK's own internal refresh timer calls, same user id, no navigation) while sampling the DOM every 25ms for ~1s. Zero samples showed the loading screen or the fallback screen across 40 samples — confirms the "same user id" branch never touches `profileLoading`. Re-ran with the background profile refetch (the one still fired for staleness) artificially delayed 1500ms and sampled every 25ms for 2.2s (87 samples) — still zero flicker, proving the no-flicker guarantee holds regardless of that background call's latency, not just when it happens to be fast. (The Supabase client instance was temporarily exposed via a dev-only `window` hook in `client.ts` to call `refreshSession()` from Playwright; removed before commit — confirmed via `git status`/`git diff` showing `client.ts` byte-identical to `HEAD` afterward.)
5. **Full regression suite re-run** (own script, same 4 scenarios QA validated on `91e94bb`/`00bcb5d`, all against the current fixed code): correct code + successful insert on first try → `/choose-plan` (PASS). Wrong/expired code → Supabase's real error text inline, no navigation (PASS). Resend cooldown → button disabled, reads `"Resend code (45s)"` immediately after signup (PASS). Genuine profile-creation failure (forced `409` on first insert after a real `verifyOtp` success) → recovery view renders with the real error text, "Log in instead" link present and pointing to `/login`, "Try again" retries only the insert (no re-verify) and succeeds on the second attempt, navigating to `/choose-plan` (PASS). All 4 hold, unchanged by this fix.

**Verification:** `npx tsc --noEmit` clean (exit 0). `npx next build` clean (exit 0, all 52 routes). `git status` confirms only `src/app/app/layout.tsx` and `src/components/providers/AuthProvider.tsx` changed — the temporary `.env.local` and the dev-only `client.ts` test hook were both removed before this check, and all scratch Playwright scripts/screenshots live outside the repo (session scratchpad).

BLOCKERS: None.

NEXT TASK: Handing back to QA to re-verify: the original login-race repro (should no longer flash the fallback at any delay), a dedicated token-refresh-flicker check (should show zero loading/fallback flicker), and a re-confirmation of the full regression suite (OTP success, wrong/expired code, resend cooldown, genuine-failure recovery view + Try again + Log in instead) — before Cato/CEO merge and deploy.

DEPLOYMENT STATUS: not deployed — committed locally on `claude/futureos-student-app-3ewdz6` (worktree `worktree-agent-adf465bc9bd0347aa`), not pushed, per instruction (QA re-verifies before merge/deploy).

DEPLOYMENT STATUS (this QA pass): not deployed — reviewed and committed locally on `claude/futureos-student-app-3ewdz6` via worktree `agent-aa76c0857f143361b`, not pushed. Do not merge/deploy — a real regression was found this pass.

---

## QA ROUND 3 — SIGNUP OTP, `AuthProvider` RACE FIX (Dev commit `f2612a6`) — 2026-08-31

Third round on this feature. This worktree (`agent-a82e19c284bd68767`) started at `e55fe07` and had none of the OTP chain on disk — fast-forward-merged `worktree-agent-adf465bc9bd0347aa` (`f2612a6`) in cleanly (`git merge --ff-only`, no conflicts) to get the full lineage before reviewing, confirmed via `git log --oneline` showing the complete chain (`5efd0e73`→`3b94a79`/`3b1f11b`→`91e94bb`/`f0d0011`→`00bcb5d`→`f2612a6`) and a direct read of `src/app/signup/page.tsx` confirming fix 1's recovery view (`verifiedUserId && error` branch, "Try again"/"Log in instead") is genuinely still present alongside fix 2's `AuthProvider`/`app/layout.tsx` changes — lineage is real, not just claimed.

**1. Diff read in full.** `git show f2612a6` — `AuthProvider.tsx` (+57/-4 net vs. pre-round-2-fix) and `app/layout.tsx` (+33/-16-ish) matches Dev's description exactly: `profileLoading` state + `lastUserIdRef`, branching `onAuthStateChange` on `nextUserId !== lastUserIdRef.current` (new/different user → `profileLoading(true)` + tracked load; same user → background-only refresh, `profileLoading` untouched; no session → resets both `lastUserIdRef` and `profileLoading`). `app/layout.tsx` gate changed to `loading || !user || profileLoading` before the `!profile` fallback.

**2. My own login-race repro, built from scratch** (own Playwright script, `qa1_login_race.mjs` — did not reuse Dev's harness). Mocked `/auth/v1/token`, `/auth/v1/logout`, `/rest/v1/profiles` via `page.route`, drove the real `/login` form, delayed the mocked profile GET by 500ms and 2000ms. Result: **0/36 and 0/108 samples showed the fallback screen** — confirmed the fallback genuinely never appears for a healthy sign-in regardless of the profile-fetch delay tested. Matches Dev's claim.

**3. Token-refresh-flicker check, own script, using a REAL `TOKEN_REFRESHED` event** (`qa2_token_refresh_flicker.mjs`) — this was the explicit constraint I set in round 2's report, and I verified Dev actually met it rather than trusting the claim. Seeded a session with `expires_at` ~3s out so supabase-js's own internal auto-refresh timer fired a genuine `POST /auth/v1/token?grant_type=refresh_token` on its own (confirmed via `refreshCount: 1` in both runs — not simulated). Sampled the DOM every 25ms across the refresh, at both 0ms and 1500ms artificial delay on the background same-user profile refetch. Result: **0 fallback hits, 0 spinner hits across 196 and 250 samples respectively.** Zero flicker, independently confirmed.

**4. `lastUserIdRef` edge cases — found a real, unreported bug here.** Traced the actual ref-comparison logic rather than assuming it's correct because the two tested scenarios passed:
- **Plain sign-out (no re-login):** `!nextUserId` branch resets `lastUserIdRef.current = null` and `setProfileLoading(false)` unconditionally — not stuck `true` forever. Confirmed by code read.
- **Sign out, sign back in as a genuinely different user:** the `nextUserId !== lastUserIdRef.current` check correctly treats the second sign-in as newly-seen (ref was reset to `null` on sign-out) and sets `profileLoading(true)` again. So far so good.
- **The bug: `loadProfile()` has no staleness guard, and this is exploitable exactly in the sign-out→different-user scenario the task asked me to check.** Built a dedicated repro (`qa3_signout_switch_user_race.mjs`) using a temporary debug route (`src/app/qa-debug-auth-xyz789/page.tsx`, deleted before commit — confirmed via `git status`/`git diff` clean afterward) that dumps raw `useAuth()` context state, plus a temporary `window.__qaSupabase` hook in `client.ts` (same technique Dev used in its own verification, also fully reverted before commit — confirmed via `git diff src/lib/supabase/client.ts` empty). Sequence: sign in as User A (profile fetch mocked at 4000ms delay) → wait 150ms (fetch still in flight) → sign out → immediately sign in as User B (profile fetch mocked at 100ms delay, resolves fast and correctly). **Result: at t+5102ms, `userId=uid-userb` but `profileId=uid-usera`, `profileFullName=Alpha Anderson` — User A's stale, late-resolving profile fetch overwrote User B's already-correctly-loaded profile in `AuthContext`, 34/~130 samples.** Root cause: the "different user" branch's `loadProfile(nextUserId).finally(() => setProfileLoading(false))` — and `loadProfile` itself — has no check that `nextUserId` is still the *current* user by the time the async response arrives. Two overlapping loads for two different users race, and whichever resolves last wins, regardless of which one is actually current.
  - **Severity:** real, not theoretical — reproduced cleanly and repeatably. `Profile` (`src/lib/types.ts`) carries `plan`, `plan_status`, `trial_ends_at`, `track`, `xp_*`, `full_name`, `avatar_emoji`, `year_group`, `country`. Checked the blast radius: `PaywallGate.tsx` gates purely on the client `profile` object (`isEntitled(profile)`), so a stale profile from a different (e.g. Plus) account could incorrectly unlock/lock the paywall UI for the live user. This is **UI-state only, not a server-side privilege escalation** — confirmed in an earlier round that `checkEntitlement` on the API side re-reads `profiles` fresh from the DB using the caller's real JWT-derived `user.id`, independent of the client's cached context object, so this can't be used to actually consume gated AI features. But it is a real, confirmed cross-account data leak in client state: on a shared/public device, or any fast account-switch flow, User B's session can end up displaying User A's name/avatar/plan/track under a live, correctly-authenticated User B session. That's a privacy/product-correctness bug regardless of the entitlement backstop, and it directly fits the scenario the task named as the highest-risk untested edge case.
  - **Fix needed (for Dev):** `loadProfile`'s `setProfile(data)` write (and the "different user" branch's `setProfileLoading(false)`) needs to check it's still the load that matters before writing — e.g. compare against `lastUserIdRef.current` (or a monotonic request-generation counter) at resolve time and no-op if it's stale: `loadProfile(userId).then(data => { if (lastUserIdRef.current === userId) { setProfile(data); } })`, and same guard on the `finally` that flips `profileLoading`. This needs to preserve the existing "same user, background refresh" behavior (still safe to write there since the id hasn't changed) — only the cross-user overlap needs guarding.
  - Verified this is **not** a confounder in Tests 2/3 above: the healthy-login and token-refresh tests only ever involve one user id at a time, so there's no ordering ambiguity to expose this bug there — it's specifically the two-different-overlapping-users case that's broken, which is why it survived two full regression passes without being caught until this round's dedicated edge-case testing.

**5. Genuine `!profile` fallback (real failure, not a race) — confirmed still works, not overcorrected into permanent hiding.** Own script (`qa4_genuine_no_profile.mjs`): seeded a session for a user whose mocked `profiles` GET returns `[]` with **zero artificial delay** (a real, completed, empty response — not a timing race). Result: fallback text appeared at t=195ms and stayed, in 70/72 samples; never stuck spinning. The `profileLoading` fix does not mask a genuine failure — it correctly resolves to `false` once the (empty) response lands, and `!profile` fires as intended.

**6. Route count — investigated, resolved as a reporting-consistency issue, not a routing bug.** Dev's "52 routes" claim and my own fresh `npx next build` ("53 routes," see below) are both well above the "42-43" figure repeated in earlier rounds' `PROJECT_STATE.md` entries. Cross-checked by counting actual `page.tsx`/`route.ts` files on disk at each historical commit via `git ls-tree`: `b73ec8a` (reported "42 routes") → **49 real files**; `e55fe07` (reported "43 routes") → **51 real files**; `f2612a6`/this round → **51 real files, unchanged from `e55fe07`** (expected — the OTP diff touches only `signup/page.tsx`, `app/layout.tsx`, `AuthProvider.tsx`, zero new route files, confirmed via `git diff e55fe07 f2612a6 --stat`). My own build's route list (53 lines) was cross-referenced 1:1 against every real `page.tsx`/`route.ts` file on disk (51) plus the two synthetic entries every Next.js build always includes (`/_not-found`, `/icon.png`) — **53 = 51 + 2 exactly, zero unexplained, duplicate, or stale/harness entries.** My temporary `qa-debug-auth-xyz789` debug route was deleted before this build ran (confirmed absent from the route list and via `git status`). Conclusion: the historical "42/43" figures under-reported the true file count by 7-8 at the time they were written (likely earlier agents excluding `/icon.png`/`/_not-found` inconsistently, or simply miscounting) — this is a **reporting-consistency problem across rounds, not a route-explosion bug introduced by this diff.** Recommend future rounds count via `find src/app -name "page.tsx" -o -name "route.ts" | wc -l` (unambiguous) rather than eyeballing the build's printed tree. Dev's own "52" vs. my measured "53" is a 1-route discrepancy of the same harmless-accounting-convention flavor, not investigated further since the file-level reconciliation already accounts for the real total exactly.

**7. Full regression suite, own script, against the real `/signup` page (`qa5_full_regression.mjs`)** — driven through the actual form (fill/click), not the SDK directly:
   - Correct code + successful profile insert → `/choose-plan` — **PASS**.
   - Wrong/expired code → Supabase's real error text (`"Token has expired or is invalid"`) shown inline, still on the code-entry screen, no navigation — **PASS**.
   - Resend cooldown → button reads `"Resend code (45s)"` and is disabled immediately after signup — **PASS**.
   - Genuine profile-insert failure (mocked `409` on first insert) → recovery view (`"Almost there"`) renders with the real error text, `"Log in instead"` link present pointing to `/login`, `"Try again"` retries only the insert (2nd attempt succeeds) and navigates to `/choose-plan` — **PASS**, all sub-checks.

**8. Build gates, run directly.** `npx tsc --noEmit`: clean (exit 0, no output). `npx next build`: clean (exit 0), 53 routes (51 real `page.tsx`/`route.ts` files + `/_not-found` + `/icon.png`, reconciled exactly per item 6). Both run after all temporary QA instrumentation (`.env.local`, the debug route, the `client.ts` window hook) was fully removed — confirmed via `git status` showing a clean tree before each gate run.

**9. Lineage — confirmed genuine**, see intro above: full commit chain present, fix 1's recovery view still byte-present in the merged `signup/page.tsx`, not silently dropped by fix 2.

**Verdict: NOT signed off.** Everything Dev's report claimed for this round checked out under my own independent re-verification: the original login-race regression is genuinely gone (own repro, two delay values, zero fallback flashes), the token-refresh-flicker constraint is genuinely met (own repro, a **real** SDK-driven `TOKEN_REFRESHED` event, zero flicker at two background-latency values), the genuine-failure fallback still works and isn't masked, the full 4-scenario OTP regression suite passes, the route count is explainable and not a real bug, and both build gates are clean. **But this round's own mandate — scrutinize `lastUserIdRef` edge cases Dev's report doesn't mention — found a real, reproducible bug Dev did not test for**: signing out and back in as a **different** user while the first user's profile fetch is still in flight causes a stale cross-account write, leaving the live, correctly-authenticated second user's `AuthContext.profile` populated with the first user's data (name/avatar/plan/track/XP) for as long as the slower fetch takes to resolve. Confirmed via a dedicated, isolated repro (temporary debug route + temporary SDK-exposure hook, both fully reverted before commit, confirmed via `git status`/`git diff`), not assumed from a code read alone. Not currently exploitable as a server-side privilege escalation (entitlement re-checks are server-side and JWT-scoped, confirmed in an earlier round), but it is a real, reproducible cross-account UI/state data leak — not deployable as-is.

**Fix needed (for Dev):** add a staleness guard to `loadProfile`'s callers in the "different user" branch of `onAuthStateChange` (and its `.finally`) — compare the resolved user id against `lastUserIdRef.current` (or a monotonic generation counter) at response time and no-op if a newer load has since superseded it. Must preserve the existing, already-verified-correct behavior for: the original login race (still must not flicker), the token-refresh case (still must not flicker), and the genuine-failure fallback (must still surface). I will re-verify with the same three own scripts (`qa1`/`qa2`/`qa3`/`qa4`/`qa5`, described above) plus specifically re-run the sign-out/different-user race at multiple delay values once the fix lands, before Cato/CEO merge and deploy.

BLOCKERS: The cross-account profile-state leak above blocks sign-off. Not deployable as-is.

NEXT TASK: Dev adds the staleness guard described above to `AuthProvider.tsx`'s `loadProfile`/`onAuthStateChange` path. QA re-verifies via the sign-out→different-user repro (multiple delay combinations) plus a full re-run of the existing regression suite (login race, token-refresh flicker, genuine-failure fallback, 4-scenario OTP suite) before handing to Cato.

DEPLOYMENT STATUS (this QA pass): not deployed — reviewed and committed locally on `claude/futureos-student-app-3ewdz6` via worktree `agent-a82e19c284bd68767`, not pushed. Do not merge/deploy — a real, reproduced cross-account data-leak bug was found this round.
